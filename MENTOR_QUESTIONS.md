# Orbit — 멘토 질문 리스트 (Upstage 멘토 기준)

> **이 문서는 멘토링 준비 시점의 질문 목록이며, 당시 계획하던 구성을 전제로 쓰였다.**
> 이후 LLM 구성은 SKT A.X-K1 ↔ LG K-EXAONE 상호 폴백으로 확정됐고 Upstage는 임베딩
> 전용으로 남았다(근거: `docs/DecisionLog.md` 2026-08-05). 아래에 나오는 Solar Pro 3 /
> Solar Mini 기반 서술은 당시 전제이며 현재 구현이 아니다. 현재 배선은 루트
> `README.md`의 "국내 AI 모델 활용" 절을 참고한다.

> 전국민 AI 경진대회 / 2인 팀
> Chrome Extension + FastAPI + Upstage `embedding-query`/`embedding-passage`

---

## 1. 임베딩 모델 선택

**Q1. `embedding-query`와 `embedding-passage`를 언제 나눠 써야 하나?**

현재 Upstage API에서 `embedding-query` (4096차원) 동작을 확인했습니다.
Orbit의 사용 패턴은 두 가지입니다.

- **저장 시**: 탭 본문 텍스트(최대 8000자) → 벡터 생성 → Qdrant 저장
- **검색 시**: 사용자 자연어 쿼리(짧은 문장) → 벡터 생성 → Qdrant 검색

저장 시에는 `embedding-passage`(문서용), 검색 시에는 `embedding-query`(쿼리용)를
각각 사용하는 비대칭 임베딩이 검색 품질을 높이는지,
아니면 Orbit 규모에서는 `embedding-query` 하나로 통일해도 충분한지 알고 싶습니다.

---

**Q2. 탭 텍스트를 그대로 임베딩할 것인가, Solar로 요약 후 임베딩할 것인가?**

탭 본문 8000자를 그대로 임베딩하는 방식과,
Solar Pro 3로 먼저 핵심 요약(500자 이내)을 만든 뒤 그 요약을 임베딩하는 방식 중
어느 쪽이 검색 정확도가 더 높을까요?

요약 후 임베딩하면 노이즈(광고 문구, 메뉴 텍스트)가 제거되는 장점이 있지만
LLM 호출이 한 번 더 늘어나는 비용이 있습니다.

---

## 2. Solar Pro 3 활용

**Q3. Solar Pro 3에서 Structured Output(JSON 강제)을 가장 안정적으로 구현하는 방법은?**

Orbit은 LLM 응답을 아래 스키마로 파싱해야 합니다.

```json
{
  "title": "세션 제목 (20자 이내)",
  "overview": "한 줄 개요",
  "purpose": "탐색 목적",
  "highlights": ["핵심 정보1", "핵심 정보2"],
  "todos": ["미완료 작업"],
  "next_actions": ["다음 행동"]
}
```

Solar Pro 3에서 Tool Calling 스키마로 강제하는 방법과
System prompt에 JSON만 출력하도록 명시 후 파싱하는 방법 중
어느 쪽이 더 안정적으로 동작하는지, 실전에서 어떻게 하시는지 알고 싶습니다.

---

**Q4. Solar Pro 3와 Solar Mini의 역할을 나누는 기준은?**

현재 모든 LLM 작업에 Solar Pro 3를 쓰려 합니다.
그런데 Orbit 내 LLM 작업들은 복잡도가 다릅니다.

- **복잡**: 탭 N개의 콘텐츠 → 탐색 목적 + 미완료 작업 추론 (세션 요약)
- **단순**: 세션 제목 20자 이내로 생성, 검색 쿼리 의도 파악

단순한 작업에 Solar Mini를 쓰면 속도와 비용을 아낄 수 있는지,
그 기준선(어느 task부터 Pro 3가 필요한지)을 어떻게 잡으면 될까요?

---

**Q5. 한국어 웹 콘텐츠의 노이즈 처리 — 프롬프트 수준에서 어떻게 다루나?**

Readability.js로 추출한 한국어 탭 텍스트에는
광고 문구, SEO 키워드 반복, 쇼핑몰 상품 목록, 유튜브 자막 노이즈 등이 섞입니다.

Solar가 이런 노이즈에 강한 편인지, 아니면 프롬프트에서
"다음은 웹 페이지 본문입니다. 광고나 메뉴 텍스트는 무시하세요" 같은 지시를
명시적으로 넣어야 효과가 있는지 알고 싶습니다.

---

**Q6. Upstage Document Parse를 Readability.js 대신 쓰는 게 나은가?**

현재 페이지 콘텐츠 추출은 Chrome Extension 내에서 Readability.js로 처리합니다.
Upstage에 Document Parse API가 있는 것으로 알고 있는데,
웹 페이지 HTML을 Document Parse로 처리하면 Readability.js보다 품질이 나아지는지,
그리고 Orbit 구조(Extension에서 추출 → 백엔드 전송)와 맞는 사용 방법인지 궁금합니다.

---

## 3. RAG 설계

**Q7. 세션 수가 적은 서비스에서 Qdrant까지 붙이는 게 적절한가?**

Orbit의 세션 수는 사용자당 수십~수백 개 수준으로 많지 않습니다.
검토 중인 대안은 LLM Reranking입니다.
세션 목록 전체(overview, purpose)를 Solar에 넘겨
"이 중 쿼리와 가장 관련 있는 세션은?"을 묻는 방식입니다.

세션 수 기준으로 벡터 DB가 실질적으로 의미 있는 시점이 어디인지,
데모·대회 환경에서 Qdrant를 도입하는 게 기술적으로 플러스 요인이 되는지 의견이 궁금합니다.

---

**Q8. HyDE(Hypothetical Document Embedding)를 데모 수준에서 도입할 가치가 있나?**

사용자가 "항공권 탭"이라고 검색하면, 저장된 세션에는 "해외여행 준비", "인천 출발"처럼
표현이 달라 벡터 유사도가 낮게 나올 수 있습니다.

HyDE — 쿼리로 가상의 세션 요약을 Solar로 먼저 만든 뒤 그 텍스트를 임베딩하는 방법 —
를 적용하면 검색 품질이 유의미하게 올라가는지,
구현 복잡도 대비 효과가 있는지 알고 싶습니다.

---

## 4. 시스템 아키텍처

**Q9. 세션 저장 시 LLM 호출을 동기로 할 것인가, 비동기로 할 것인가?**

"저장" 버튼 클릭 → Solar Pro 3 요약 생성 → 응답 반환까지 3~10초가 걸릴 수 있습니다.

- **동기**: 구현 단순, but 사용자가 대기
- **비동기**: 즉시 저장 완료 → 백그라운드 요약 → 완료 시 Extension에서 polling

데모 환경에서 대기 UX가 감점 요인이 될 수 있는지,
Solar Pro 3의 실제 응답 속도(탭 10개 기준)가 어느 정도인지 알면 판단에 도움이 됩니다.

---

**Q10. Solar 호출을 Chrome Extension에서 직접 해도 되는가, 백엔드를 반드시 거쳐야 하나?**

Extension에서 Upstage API를 직접 호출하면 백엔드 없이 구현이 단순해집니다.
그러나 API 키가 Extension 번들에 포함되는 보안 문제가 있습니다.

대회 데모 환경에서 이 방식이 현실적인 선택지인지,
Upstage 측에서 권장하는 사용 패턴이 있는지 알고 싶습니다.

---

**Q11. 탭 콘텐츠를 Solar API로 전송할 때 개인정보 처리 수준은?**

사용자의 모든 탭 텍스트가 Upstage API로 전송됩니다.
금융, 의료, 로그인 페이지 등 민감한 내용이 포함될 수 있습니다.

- `chrome://`, `about:` 등 시스템 URL 제외
- 금융·의료 도메인 블랙리스트
- 전송 전 사용자 동의

대회 심사에서 개인정보 처리가 평가 항목인지,
Upstage API 이용 약관상 어떤 수준의 처리가 필요한지 알고 싶습니다.

---

## 5. 대회 전략

**Q12. Upstage 모델을 어떻게 활용해야 심사에서 높게 평가받나?**

Orbit의 Solar 활용:
- Solar Pro 3로 세션 요약 + 이름 생성
- embedding-query + Qdrant로 자연어 검색 (RAG)
- Solar Mini로 경량 task 처리 (검토 중)

단순히 모델을 호출하는 것과, 파이프라인 설계(임베딩 → 클러스터링 → 요약 → RAG) 측면에서
어느 부분이 "Upstage를 잘 활용했다"는 평가를 받는 포인트인지 알고 싶습니다.

---

**Q13. 데모에서 Solar의 강점을 가장 잘 보여줄 수 있는 시나리오는?**

현재 보여줄 수 있는 기능:
1. 탭 묶음 → Solar Pro 3가 한국어로 세션 요약 + 이름 자동 생성
2. "어제 보던 여행 탭 찾아줘" → embedding-query + Qdrant로 세션 검색 후 복원
3. 세션별 탐색 목적 / 미완료 작업 / 다음 행동 표시

이 중 Solar의 한국어 능력이 가장 돋보이는 시나리오가 어떤 것인지,
심사위원 입장에서 인상적인 순서가 있는지 조언 부탁드립니다.

---

**Q14. 2인 팀 기준 데모까지 현실적으로 구현해야 할 최소 범위는?**

현재 완성:
- Chrome Extension UI + 로컬 저장/복원/검색 (mock 데이터)

미구현:
- 백엔드 Solar 요약 파이프라인
- Extension ↔ Backend 연동
- embedding-query + Qdrant 검색

3단계 모두 완성이 필요한지, 아니면 Solar 요약(1단계)만 붙이고
벡터 검색은 시나리오로 설명해도 평가에서 불이익이 없는지
우선순위 관점에서 의견을 듣고 싶습니다.
