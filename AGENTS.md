# AGENTS.md

이 프로젝트에서 작업하는 모든 개발자와 AI 에이전트는 아래 규칙을 따른다.

## 프로젝트 개요와 공식 명령

Orbit은 브라우저 탐색 이벤트를 opt-in 상시 수집해 세션으로 자동 재구성하는
Personal Exploration Memory 서비스다. 저장소는 세 파트로 구성된다.

* `extension/` — Chrome MV3 사이드패널. WXT + React 19 + TypeScript + Tailwind v4.
  이벤트 수집기·로컬 큐·동기화 엔진은 `lib/events`, `lib/sync`.
* `backend/` — FastAPI + SQLAlchemy(async) + PostgreSQL + Qdrant. 이벤트 인제스트,
  배치 세션화, 세션 API, AI 파이프라인(`app/services`).
* `frontend/` — 웹 대시보드. React 19 + Vite + TypeScript + Tailwind v4.

로컬 기동은 `./dev.sh`(Docker 포함 일괄) 또는 `./dev_conda.sh`를 사용한다.
인프라(postgres, qdrant)는 `docker-compose.yml`로 관리한다.

파트별 공식 검증 명령:

```bash
# Backend — pytest 사용 (unittest 아님)
cd backend && python -m pytest -p no:asyncio

# Extension — 타입 검사 + 빌드
cd extension && pnpm compile && pnpm build

# Frontend — 빌드에 타입 검사(tsc --noEmit) 포함
cd frontend && pnpm build
```

`backend/eval/`의 평가 하네스(`python -m eval.run_eval`)는 실제 LLM API를 호출해
비용이 발생하므로 사용자가 요청한 경우에만 실행한다.

## 1. 기본 원칙

* 변경 전에 기존 코드, 문서, 프로젝트 구조를 먼저 확인한다.
* 현재 구조와 컨벤션을 우선적으로 따른다.
* 문제를 해결하는 가장 작고 안전한 변경을 한다.
* 요청받지 않은 리팩터링이나 코드 정리는 하지 않는다.
* 복잡한 구조보다 읽기 쉽고 유지보수 가능한 구현을 우선한다.
* 확인하지 않은 내용은 추측하지 않는다.
* 사용자 지시, 이 문서, 기존 프로젝트 결정 순으로 우선한다.

## 2. 작업 순서

코드 변경은 다음 순서로 진행한다.

1. 관련 코드, 문서, 테스트, 실행 명령을 조사한다.
2. `docs/Plan.md`를 작성하거나 갱신한다.
3. 필요한 경우 사용자 결정을 받는다.
4. 계약, 타입, 스키마를 먼저 정리한다.
5. 계획에 따라 최소 범위로 구현한다.
6. 테스트, 린트, 타입 검사, 빌드를 실행한다.
7. 관련 문서와 `docs/WorkLog.md`를 갱신한다.
8. 변경 파일과 검증 결과를 최종 보고한다.

조사와 오류 재현은 `Plan.md` 작성 전에도 가능하지만, 실제 코드 수정은 계획 작성 후 시작한다.

오타 수정, 문서 수정, 한 파일 안의 소규모 버그 수정처럼 14장의 "큰 변경" 기준에
해당하지 않는 작업은 `Plan.md` 작성·갱신을 생략할 수 있다. 이 경우에도 검증 실행과
`WorkLog.md` 기록은 유지한다.

## 3. Plan.md

`docs/Plan.md`에는 최소한 다음 내용을 포함한다.

* 작업 목표
* 현재 상태와 조사 결과
* 포함 범위와 제외 범위
* 변경할 파일 또는 모듈
* 구현 순서
* 테스트 및 검증 방법
* 위험과 사용자 결정이 필요한 사항
* 완료 조건

작업 범위가 바뀌면 계획도 함께 갱신한다.

## 4. 사용자 결정이 필요한 경우

다음 사항은 임의로 결정하지 않는다.

* 기술 스택 또는 주요 라이브러리 변경
* API, CLI, 설정 형식 변경
* 데이터 모델, 스키마, 저장 방식 변경
* 인증, 권한, 보안 정책 변경
* 유료 서비스 또는 운영 비용이 발생하는 선택
* 기존 기능 삭제나 비호환 변경
* 제품 범위나 사용자 흐름에 영향을 주는 결정

질문할 때는 선택지, 장단점, 추천안을 함께 제시한다.

함수명, 내부 파일 배치처럼 영향이 작고 쉽게 되돌릴 수 있는 사항은 기존 컨벤션에 따라 판단할 수 있다.

## 5. 필수 문서

다음 문서는 유지해야 한다.

* `docs/ProjectContext.md` — 프로젝트 배경, 목표, 제약
* `docs/Plan.md` — 구현 계획
* `docs/IA.md` — 화면 및 정보 구조
* `docs/UserScenarios.md` — 사용자 시나리오
* `docs/Personas.md` — 사용자 유형
* `docs/DecisionLog.md` — 주요 결정과 근거
* `docs/WorkLog.md` — 작업, 오류, 수정 및 검증 이력
* `docs/Process.md` — 작업 프로세스
* `docs/ReviewChecklist.md` — 완료 전 체크리스트
* `docs/Research.md` — 기술 및 라이브러리 조사

누락된 문서는 관련 작업을 시작할 때 생성한다.

`IA.md`, `UserScenarios.md`, `Personas.md`는 서로 합치지 않는다.

현재 제품 방향과 설계의 기준은 다음 문서이며, 관련 영역을 변경하기 전에 먼저 확인한다.

* `docs/product-direction-v2.md` — 제품 방향과 v2 전환 근거
* `docs/target-architecture.md` — 목표 아키텍처
* `docs/api-design-v2.md` — API 설계
* `docs/data-model-v2.md` — 데이터 모델

## 6. 문서 기록 기준

* 프로젝트 목표나 방향 변경: `ProjectContext.md`
* 구현 계획: `Plan.md`
* 주요 기술·정책 결정: `DecisionLog.md`
* 작업 내용, 오류, 원인, 해결 과정: `WorkLog.md`
* 기술이나 라이브러리 조사: `Research.md`

API, CLI, 설정 스키마, 화면 흐름을 바꾸면 관련 README와 설계 문서도 같은 작업에서 갱신한다.

## 7. 코드 품질

* 언어가 지원하면 명시적인 타입을 사용한다.
* 함수와 모듈은 하나의 책임에 집중한다.
* 이름은 의도가 드러나게 작성한다.
* 중복 로직은 재사용 가능한 단위로 분리한다.
* 필요가 입증되지 않은 추상화는 추가하지 않는다.
* 사용하지 않는 코드, import, 임시 로그를 제거한다.
* 코드 내용을 그대로 반복하는 주석은 작성하지 않는다.
* 주석은 비명시적인 제약이나 설계 이유에만 사용한다.

## 8. 아키텍처

* UI, 비즈니스 로직, API, 데이터 계층의 경계를 유지한다.
* UI 컴포넌트에는 복잡한 도메인 로직을 넣지 않는다.
* 외부 서비스는 가능한 한 어댑터나 서비스 계층 뒤에 둔다.
* 전역 상태와 새로운 의존성은 필요한 경우에만 도입한다.
* 여러 모듈이 의존하는 변경은 타입, 인터페이스, 스키마 등 계약을 먼저 확정한다.

## 9. 설정과 데이터

* 비밀값과 환경별 값은 환경변수로 관리한다.
* `.env`, 토큰, 키, 인증정보, 로컬 설정을 커밋하지 않는다.
* 필요한 설정은 `.env.example` 또는 README에 기록한다.
* 외부 입력은 시스템 경계에서 검증한다.
* 누락, 중복, 잘못된 형식의 데이터를 안전하게 처리한다.
* 데이터 모델, API 스키마, UI 타입을 일치시킨다.
* 저장 구조 변경에는 마이그레이션이나 변경 절차를 포함한다.

## 10. 오류 처리

* loading, empty, success, error 상태를 처리한다.
* 오류를 숨기거나 성공처럼 처리하지 않는다.
* 사용자에게 내부 스택이나 비밀정보를 노출하지 않는다.
* 외부 서비스에는 타임아웃과 제한된 재시도를 적용한다.
* 클라이언트 오류, 내부 오류, 외부 서비스 오류를 구분한다.
* fallback이 원래 실패를 숨기지 않도록 로그와 상태를 남긴다.

## 11. AI 및 외부 서비스

* AI와 외부 API 응답은 신뢰하지 않는다.
* 구조화된 출력은 스키마로 검증한다.
* 빈 응답, 잘린 응답, 잘못된 형식에 fallback을 둔다.
* 프롬프트, 스키마, 파싱 로직은 가능한 한 중앙화한다.
* AI 출력으로 파일 삭제, 배포, 데이터 변경 등 파괴적 작업을 자동 실행하지 않는다.
* API 키와 민감정보를 프롬프트, 로그, 오류 응답에 노출하지 않는다.

## 12. 테스트와 검증

* 핵심 비즈니스 로직에는 테스트를 추가하거나 갱신한다.
* 정상 경로뿐 아니라 실패와 경계 조건도 테스트한다.
* 외부 API, 데이터베이스, AI 호출은 테스트에서 대역 처리한다.
* 테스트는 실제 키나 네트워크에 의존하지 않는다.
* 버그는 최소 재현으로 원인을 확인한 뒤 수정한다.
* 테스트를 통과시키기 위해 테스트를 삭제하거나 약화하지 않는다.

변경 후 가능한 범위에서 다음을 실행한다.

1. 관련 테스트
2. 전체 테스트
3. 타입 검사
4. 린트
5. 빌드
6. 통합 스모크 테스트

실행하지 않은 검증을 실행했다고 보고하지 않는다.

## 13. 병렬 작업

서브에이전트를 사용할 때는 다음을 지킨다.

* 공통 계약을 먼저 확정한다.
* 에이전트별 수정 파일이 겹치지 않게 한다.
* 읽을 파일, 수정 범위, 검증 명령을 명확히 전달한다.
* 아키텍처, 통합, 핵심 계약은 직접 관리한다.
* 결과는 그대로 병합하지 않고 diff와 테스트를 다시 검토한다.

큰 작업 완료 후에는 다음 관점으로 검토한다.

* 코드 오류와 실패 경로
* 신규 사용자의 설치 및 사용 흐름
* 코드와 문서의 일치 여부

## 14. Git 규칙

큰 변경은 `main`에 직접 커밋하지 않는다.

큰 변경 기준:

* 새 기능
* 여러 파일에 걸친 코드 변경
* 구조, 스키마, 설정 형식 변경
* 마일스톤 단위 작업

브랜치 이름:

* `feat/<name>`
* `fix/<name>`
* `refactor/<name>`
* `docs/<name>`
* `test/<name>`
* `chore/<name>`

PR에는 변경 요약과 테스트 결과를 포함한다.

병합은 squash merge를 사용하고, 병합 후 브랜치를 삭제한다.

문서만 수정하는 경우에는 `main` 직접 커밋이 가능하다.

push, PR 생성, 병합, 원격 브랜치 삭제는 사용자가 명시적으로 요청한 경우에만 수행한다.

## 15. Git 산출물의 AI 흔적 금지

커밋 메시지, PR, 브랜치명에 다음 내용을 포함하지 않는다.

* `Co-Authored-By: Claude`
* `Generated by AI`
* AI 도구 서명이나 링크
* 로봇 이모지
* AI 도구 이름이 포함된 브랜치명

커밋 메시지는 Conventional Commits 형식을 사용한다.

예:

```text
feat(router): add provider failover
fix(stream): handle truncated responses
docs(config): document provider settings
```

## 16. 환경 규칙

로컬 개발 환경은 Windows 11이다. 프로젝트 스크립트(`dev.sh`, `dev_conda.sh`)는
bash(Git Bash) 전용이며, PowerShell 5.1에서는 `&&` 체이닝과 복잡한 큰따옴표 인자를
피한다.

* Python 환경은 `backend/.venv`(dev.sh가 자동 감지) 또는 프로젝트 로컬
  conda(`.conda`, dev_conda.sh 사용 시)를 쓰고, 둘 다 없으면 시스템 Python으로 동작한다.
* 테스트·타입 검사·빌드는 "프로젝트 개요와 공식 명령"의 파트별 명령을 사용한다.
* 공식 프로젝트 명령이 있으면 해당 명령을 우선한다.
* 사용자 노출 CLI, 로그, 오류 메시지는 영어 ASCII를 우선한다.
* 요청 처리 경로에서 블로킹 I/O를 직접 실행하지 않는다.
* API 키는 로그, 오류, 응답, 대시보드에 노출하지 않는다.

## 17. 완료 조건

다음 조건을 충족해야 작업이 완료된 것으로 본다.

* 요청한 문제가 해결되었다.
* 요청하지 않은 변경이 포함되지 않았다.
* 계획과 실제 구현이 일치한다.
* 관련 테스트와 검증이 완료되었다.
* 실패 경로가 처리되었다.
* 임시 코드와 디버그 로그가 제거되었다.
* 관련 문서가 코드와 일치한다.
* `WorkLog.md`가 갱신되었다.
* 주요 결정이 `DecisionLog.md`에 기록되었다.
* 비밀값, 생성 파일, 로컬 파일이 커밋에 포함되지 않았다.

## 18. 최종 응답 형식

작업 완료 후 다음을 보고한다.

* 변경 요약
* 수정한 파일
* 실행한 명령과 결과
* 갱신한 문서
* 가정한 내용
* 남은 위험과 TODO

실행하지 않은 작업이나 확인하지 않은 결과는 솔직하게 명시한다.

## 19. 서브에이전트 및 모델 운영

- 작은 변경에는 서브에이전트를 사용하지 않는다.
- 일반 작업은 동시에 최대 2개, 큰 마일스톤은 최대 3개까지만 실행한다.
- 같은 조사나 리뷰를 여러 에이전트에게 중복 위임하지 않는다.
- 서브에이전트의 중첩 위임은 금지한다.
- 같은 파일을 여러 에이전트가 동시에 수정하지 않는다.

### 모델과 Effort 선택

- 빠른 모델 + `low`: 파일 탐색, 검색, 분류, 로그 및 문서 요약
- 균형형 모델 + `medium`: 일반 구현, 테스트, 코드 리뷰, 버그 수정
- 고성능 모델 + `high`: 복잡한 다중 파일 변경, 아키텍처 및 통합 검토
- `xhigh` 이상은 다른 단계로 해결하지 못한 고난도 작업에만 사용한다.
- 가장 높은 effort를 여러 에이전트에 동시에 사용하지 않는다.
- 기본값은 메인 에이전트 `medium`, 조사 에이전트 `low`로 한다.

### 위임 규칙

서브에이전트에는 다음을 명확히 전달한다.

- 작업 목표
- 읽을 파일과 디렉터리
- 수정 가능한 파일
- 수정 금지 파일
- 따라야 할 타입과 계약
- 검증 명령
- 짧은 최종 보고 형식

탐색과 문서 검토는 가능한 한 읽기 전용으로 수행한다.
서브에이전트 결과는 메인 에이전트가 diff와 테스트 결과를 검토한 뒤 반영한다.