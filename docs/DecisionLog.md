# Orbit 결정 기록

주요 기술, 데이터, 보안, 제품 결정을 시간순으로 기록한다. 새 결정은 기존 항목을 삭제하지 않고 상태를 변경하거나 후속 항목으로 남긴다.

## 결정 형식

- 날짜:
- 상태: 제안 / 승인 / 대체 / 폐기
- 배경:
- 결정:
- 대안:
- 근거와 영향:

## 2026-08-12 — 타임라인은 "방문 기록 대체"로 확정하고 상단 카드를 표면에 흡수시킨다

- 상태: 승인
- 배경: 타임라인 상단에 `SyncStatusCard`(수집 상태 + 오늘 방문/미처리/마지막 동기화 + 지금 저장)가,
  하단에 `이번 주 탐색 분석` 카드가 상시로 붙어 있었다. 두 카드가 세로 약 160px를 먹어
  목록이 5~6줄 밀렸다. 조사 중 목록의 원천이 서버가 아니라 로컬 큐(48시간 보관)라는 것과,
  분석 카드만 7일치를 보고 있어 한 화면 안에서 시간 범위가 어긋나 있다는 것을 확인했다.
- 결정:
  - 타임라인의 역할을 **브라우저 방문 기록의 대체**로 확정한다. 최근 48시간을 훑고 즉시
    재진입하는 화면이며, 오래된 기록은 세션 탭과 Ask가 맡는다.
  - `이번 주 탐색 분석` 카드를 제거한다.
  - `SyncStatusCard`를 해체해 세 기능을 각각 기존 표면에 흡수시킨다.
    - 지금 저장 → 상단 바 새로고침 아이콘에 통합(동기화 요청 + 쿼리 무효화)
    - 미처리·마지막 동기화 → sticky 날짜 헤더 우측(맨 위 그룹에만)
    - 수집 opt-in 안내 → 조건부. 기록 없으면 전체 화면, 있으면 한 줄, 켜져 있으면 없음
  - 로컬 필터 입력을 추가한다.
  - 연속된 같은 도메인 방문 뭉치기는 이번에 하지 않는다.
- 대안:
  - 상태 카드를 얇은 스트립으로 축소 → 여전히 상시 행 하나를 소비한다. 날짜 헤더가 어차피
    sticky라 그 자리를 겸하면 추가 비용이 0이다.
  - 지금 저장을 타임라인 헤더에 따로 두기 → 사용자가 새로고침 아이콘에 기대하는 동작과
    중복된다. 버튼 두 개가 사실상 같은 의도를 가진다.
  - 검색을 Ask에만 맡기기 → Ask는 서버 데이터만 본다. 방금 본 페이지는 아직 로컬 큐에
    `분류 대기`로 있어 검색 사각지대가 된다. "5분 전에 본 것"이 가장 자주 찾는 대상이라
    치명적이다.
- 근거와 영향:
  - 상단 카드 제거로 첫 화면 목록 노출이 5~6줄 늘어난다.
  - 동기화 상태는 정상일 때 시각만, 문제일 때 점+개수로 나타난다. 상시 3열 숫자판보다
    신호 대 잡음이 낫다.
  - 필터는 로컬 배열 연산이라 네트워크를 타지 않고 즉시 반응하며, Ask(의미 검색·과거)와
    역할이 겹치지 않는다.
  - 타임라인을 전체 기록 열람으로 넓히려면 백엔드에 기간 조회를 새로 열어야 한다. 미결정으로
    남긴다.

## 2026-08-12 — 사이드패널에서 수동 세션 저장을 없애고 Ask AI를 상시 독으로 내린다

- 상태: 승인
- 배경: 세션 탭 상단에 `현재 세션` 카드가 있어 "지금 열린 탭을 세션으로 저장" 버튼을 제공했다.
  세션화는 이미 이벤트 배치가 담당하므로 이 버튼은 같은 결과를 다른 경로로 한 번 더 만드는
  중복 입구였다. 한편 Ask AI는 상단 세그먼트 탭 3개 중 하나를 차지해, 다른 탭을 보는 도중에
  질문하려면 탭을 옮겨야 했다.
- 결정:
  - `현재 세션` 카드와 수동 저장 경로(`useSaveSessionsClustered`, 클러스터링 진행 표시)를 UI에서 제거한다.
  - Ask AI를 탭에서 빼고 메인 탭(타임라인/세션/열린 탭) 하단에 상주하는 입력창으로 내린다.
    질문을 보내면 답변 화면이 그 위로 올라오고, 상단 바가 `‹ Ask AI` 헤더로 바뀐다.
  - 비워진 세 번째 탭에 기존 `열린 탭 찾기·북마크`를 올린다.
  - 세션 목록은 카드 그리드에서 행 리스트로 바꾸고, 요약·탭 목록·복원·삭제는 caret 펼침 안에 둔다.
- 대안:
  - Ask AI 탭 유지 + 하단 독 추가 → 같은 기능의 입구가 둘이 되어 탭 자리만 낭비한다.
  - 행 hover 시 복원 버튼 노출 → 사이드패널 폭에서 파비콘 스택과 자리가 겹친다.
  - 복원·삭제를 상세 뷰로만 밀기 → 자주 쓰는 복원이 2단계가 된다.
- 근거와 영향:
  - 검색 경로는 원래부터 Ask 하나뿐이었다(`useAskConversation` 이 `search_memory` /
    `search_session` / `navigate_tab` 으로 라우팅). 채팅창으로 내려도 잃는 검색 기능이 없다.
  - 대화 턴은 모듈 전역 store 에 있어 탭을 옮겨도 유지된다. 답변 화면은 언마운트하지 않고
    transform 으로만 오르내려 스크롤 위치를 지킨다.
  - 하단 독이 세로 약 60px 을 상시 점유하고 행 높이도 커져, 한 화면에 보이는 세션 수가 줄어든다.
  - `lib/api.ts` 의 `saveSessionsClustered` 와 백엔드 `/sessions/cluster` 는 남겨 둔다.
    API 경계까지 함께 지우는 것은 이번 UI 변경의 범위를 넘는다.

## 2026-08-07 — 세션 정리는 자동 클러스터링이 아니라 사용자 폴더로 한다

- 상태: 승인
- 배경: 대시보드의 세션 목록이 시간축 단일 정렬이라 기록이 쌓이면 스크롤로만 찾게 된다.
  주제 축을 하나 더 주는 2뎁스가 필요했다. `atlas/data.ts` 에는 "백엔드에 Orbit 엔티티가
  없어 세션을 중심 노드로 직접 쓴다"는 주석이 남아 있었고, 캔버스의 궤도는
  `PAGES_PER_ORBIT = 8` 로 페이지를 자르는 순수 페이지네이션이라 의미가 없었다.
- 결정:
  - 주제 그룹은 **사용자가 직접 만드는 폴더**로 한다. 자동 클러스터링을 쓰지 않는다.
  - 세션은 폴더 하나에만 속한다(단일 소속, `sessions.folder_id`).
  - 폴더는 백엔드에 저장한다(`folders` 테이블).
  - 캔버스 폴더 씬은 3계층 — 중심=폴더, 궤도 한 줄=세션, 그 궤도의 점=해당 세션의 페이지.
  - 궤도는 앞면 7개만 노출하고 나머지는 뒤편에 보관해 회전으로 접근한다.
- 대안:
  - 자동 임베딩 클러스터링 — 정리 노동이 0이지만 오분류가 필연이다. 2뎁스에서 오분류는
    "찾는 세션이 엉뚱한 서랍에 들어가 보이지 않는" 실패가 되어 재발견 도구에 치명적이다.
  - 다중 소속(태그형) — 애매한 세션을 버리지 않아도 되지만 드래그가 이동인지 복사인지
    모호해지고 폴더에서 빼는 UI가 따로 필요해진다.
  - 확장 로컬 저장(chrome.storage) — 백엔드 변경이 없지만 기기가 바뀌면 정리가 사라지고,
    서버에서 세션이 병합·삭제되면 고아 참조가 생긴다.
  - 궤도 = 시간축, 점 = 세션 — 밀도 통제는 가장 좋지만 사용자가 원한 주제 축이 아니다.
- 근거와 영향:
  - 수동 정리는 오분류가 없고, 사용자가 왜 이렇게 묶였는지 항상 안다.
  - 오분류 위험을 낮추는 대신 정리 노동이 생기므로, 검색은 폴더 계층을 무시한 평면 결과를
    유지해 정리하지 않아도 기록을 찾을 수 있게 한다.
  - 궤도 정원(14개)과 동시 렌더 궤도 수(4개)로 밀도를 통제한다. 이 상한이 없으면
    세션 5개 x 페이지 30개에서 점 150개가 겹쳐 개별 점을 누를 수 없다.
  - 화살표 4방향이 궤도 회전·축 이동으로 재배정되어 기존 세션/페이지 순회 단축키를 대체했다.

## 2026-08-07 — 복수 열린 탭은 자동 선택하지 않고 사용자 후보 선택으로 전환

- 상태: 승인
- 배경: 같은 YouTube 사이트의 탭이 두 개 열리면 의미 점수 격차가 작아 자동 이동이 안전하게
  보류됐지만, 기존 UI는 더 구체적으로 말하라는 안내만 제공해 사용자가 다시 제목을 입력해야 했다.
- 결정:
  - 단일 확실 후보는 기존처럼 자동 이동한다.
  - 로컬 정확 매칭이 여러 개이거나 의미 후보 top-1 점수는 기준을 통과했지만 격차가 작으면 최대 3개를
    Ask 답변 안에 제목·호스트/경로 버튼으로 표시한다.
  - 의미 후보의 절대 점수가 기준보다 낮으면 관련 없는 후보를 노출하지 않는다.
  - 클릭 시 현재 열린 탭 ID를 다시 조회하고, 닫혔으면 실행하지 않고 후보별 오류를 표시한다.
- 대안: 첫 번째 또는 활성 탭 자동 선택, 사용자가 문장을 다시 입력, 별도 modal.
- 근거와 영향: 탭 이동의 오동작 방지 게이트를 유지하면서 한 번의 클릭으로 모호성을 해소한다.
  실제 두 YouTube 후보 HTTP smoke에서 `low_confidence`와 후보 2개를 반환했다.

## 2026-08-07 — Ask AI는 네 의도 라우팅과 요청 시점 이벤트 랭킹을 사용

- 상태: 승인
- 배경: 기존 Ask는 탭 이동만 별도 판별하고 나머지 입력은 모두 세션 top 3 RAG로 처리했다.
  세션 자체를 찾는 요청과 세션 안의 사실을 묻는 요청이 구분되지 않았고, 페이지 근거도 현재 질문이
  아니라 저장 당시 relevance와 체류 시간으로 선택됐다.
- 결정:
  - Ask 입력을 `navigate_tab`, `find_sessions`, `search_memory`, `search_session`으로 구분한다.
  - 명확한 결과 형태는 규칙으로, 나머지는 query/passage 임베딩 prototype으로 판별한다.
    retrieval 의도가 모호하면 `search_memory`로 fallback한다.
  - 탭 이동은 통합 라우터와 기존 탭 후보 resolver의 이중 score/margin 게이트를 모두 통과해야 한다.
  - 세션 찾기는 최대 5개의 관련 세션을 반환하고 답변 LLM을 호출하지 않는다.
  - 전체 기록 내용 검색은 관련 세션 3개, 특정 세션 검색은 지정 세션 또는 가장 가까운 세션 1개를 사용한다.
  - 세션별 이벤트 후보를 기존 relevance로 12개까지 제한한 뒤 질문과 passage 임베딩 유사도로 최대 4개를
    선택한다. 이벤트 벡터는 저장하지 않고 장애 시 기존 랭킹으로 fallback한다.
- 대안: 모든 입력을 LLM tool calling으로 분류, 정규식만 확장, 이벤트 벡터를 별도 Qdrant 컬렉션에 영구 저장.
- 근거와 영향: 실제 Upstage 42문장 골든셋에서 42/42, 탭 이동 오탐 0건이었다. 현재 DB 이벤트
  20개 probe에서는 기대 페이지 hit@1 17/20, hit@3 20/20이었다. 요청당 라우터 임베딩과 내용 질문의
  이벤트 batch 임베딩 비용이 추가되지만 스키마 변경 없이 질문 관련 근거 품질을 높인다.

## 2026-08-07 — 열린 탭 작업은 extension 로컬, 북마크는 명시적 선택으로 제한

- 상태: 승인
- 배경: 사용자가 열려 있는 탭을 빠르게 찾아 원래 창으로 이동하고, 선택한 열린 탭을
  Chrome 기본 북마크에 자동 추가하는 기능을 요청했다.
- 결정:
  - 모든 일반 창의 비시크릿 탭을 extension에서 직접 조회하고 제목·URL substring으로 찾는다.
  - 이동은 `windows.update(focused)` 후 `tabs.update(active)`를 호출한다.
  - `bookmarks` 권한을 manifest에 추가한다. 체크박스로 선택한 탭과 명시적 북마크 버튼 클릭에만
    쓰며, 생성 위치는 Chrome 기본값인 ‘기타 북마크’다.
  - 같은 선택 내 중복 URL과 이미 존재하는 북마크 URL은 새로 만들지 않는다.
- 대안: 백엔드에 열린 탭을 전송해 검색, 북마크바 고정 저장, 모든 열린 탭 자동 북마크.
- 근거와 영향: 탭 위치와 북마크는 브라우저 로컬 상태이므로 백엔드·LLM 없이 빠르고 예측 가능하게
  처리한다. 대신 `bookmarks` 권한은 설치 시 “북마크 읽기 및 변경” 경고를 추가하므로
  기능 설명과 사용 시점을 명확히 노출한다.

## 2026-08-07 — 자연어 열린 탭 이동은 로컬 정확 매칭 + 비저장 의미 resolver

- 상태: 승인
- 배경: `탭 + 이동` 정규식과 한글 서비스 별칭은 표현이 조금만 달라도 실패했다. 사용자는 임베딩
  유사도 검색과 실제 데이터 검증을 요청했다.
- 결정:
  - 정확한 제목·URL 매칭은 extension 로컬에서 즉시 처리한다.
  - 이동·전환·돌아가기·이어 보기 가능성이 있는 간접 표현만 `POST /tab-actions/resolve`로 보낸다.
  - query는 `embedding-query`, 이동/질문/새 검색 prototype과 열린 탭 후보는
    `embedding-passage`로 계산한다. 탭 제목·호스트·path와 알려진 사이트의 일반 용도를 사용한다.
  - 이동 의도 score/margin과 후보 top-1 score/top-2 margin을 모두 통과해야 자동 이동한다.
    기본값은 각각 `0.14`, `0.02`, `0.20`, `0.06`이다.
  - 후보 제목·URL·벡터는 DB·Qdrant·로그에 저장하지 않는다. URL query와 fragment는 passage에서 제외한다.
    `민감 도메인 제외`가 켜진 URL은 의미 resolver로 전송하지 않는다.
  - extension은 backend가 고른 ID가 현재 목록에 남아 있는지 다시 확인한 뒤 실행한다.
- 대안: 정규식·별칭만 확대, 모든 Ask 입력을 LLM tool router에 전달, 열린 탭을 Qdrant에 영구 저장.
- 근거와 영향: 실제 Upstage 골든셋 21개에서 이동 10/11, 비이동·후보 없음 차단 10/10으로
  총 20/21이었다. 남은 실패는 오이동이 아니라 낮은 후보 격차로 인한 보류였다. 탭 상태는
  일시적이므로 영구 벡터 저장보다 요청 시점 batch 임베딩이 데이터 수명과 맞다.

## 2026-08-07 — 명시적 탭 이동 문장은 Ask AI의 로컬 명령으로 우선 처리

- 상태: 대체 — 위 ‘로컬 정확 매칭 + 비저장 의미 resolver’ 결정으로 확장
- 배경: 수동 열린 탭 도구보다 “지금 열린 GitHub 탭으로 이동해줘” 같은 자연어 호출이 핵심
  사용 흐름이며, 세션 목록에서는 보조 도구가 항상 공간을 차지하지 않아야 한다.
- 결정:
  - 새 탭과 사이드패널이 공유하는 Ask AI 훅에서 `탭`과 이동·전환·열기 표현이 함께 있는
    명시적 명령만 로컬 의도로 판별한다.
  - 제목 정확 일치, 호스트 정확 일치, 제목 시작·포함, 호스트 포함, URL 포함 순으로 점수를 매겨
    가장 가까운 탭을 선택한다. 복수 일치 여부는 답변에 표시한다.
  - 로컬 탭 명령은 백엔드나 LLM에 전달하지 않으며 실행 결과만 기존 대화 목록에 누적한다.
  - 세션 화면의 수동 열린 탭 검색·북마크 도구는 기본 접힘 상태로 유지한다.
- 대안: 모든 질문을 LLM에 보내 도구 호출 생성, 복수 결과마다 사용자 재선택, 수동 패널만 유지.
- 근거와 영향: 제한된 의도만 결정론적으로 처리해 오인 실행과 지연을 줄인다. 탭 이동은 데이터 변경이
  없는 가역 동작이라 복수 일치 시 자동 선택하며, 북마크 생성은 계속 명시적 체크와 버튼으로 제한한다.

## 2026-08-07 — Ask AI는 독립 단일턴을 화면에 누적

- 상태: 승인
- 배경: 새 탭 AI 모드와 사이드패널 Ask AI가 실제 답변을 만들지 않고 검색 결과로 이동하거나
  세션/이벤트 목록만 보여주고 있었다. 사용자는 스트리밍 답변과 관련 세션 출처를 요청했다.
- 결정:
  - `POST /ask/stream`의 SSE 이벤트를 `sources → delta* → done|error`로 고정하고,
    새 탭과 사이드패널이 같은 파서와 대화 훅을 사용한다.
  - 답변 근거는 기존 벡터 검색으로 찾은 세션 최대 3개, 세션 요약, 세션별 실제 이벤트 본문
    조각 최대 4개로 제한한다. 답변의 `[n]`과 UI의 관련 세션 번호를 일치시킨다.
  - 각 질문은 이전 질문·답변을 전달하지 않는 독립 단일턴으로 처리한다. 질문·답변 목록은
    extension 문서 수명의 공용 상태에 누적해 화면 전환 후에도 유지하고,
    `새 대화 시작하기`에서만 진행 중 요청과 목록을 초기화한다.
  - 새 탭에서는 AI 모드에만 누적 대화 패널을 표시한다. 검색 모드로 바꾸면 상태를 지우지 않고
    기존 홈 콘텐츠를 표시하며, AI 모드로 돌아오면 같은 대화를 다시 보여준다.
  - A.X-K1을 우선 스트리밍하고 첫 토큰 전 실패에만 EXAONE으로 폴백한다. 출력 후 장애는
    부분 답변 오류로 명시해 두 모델의 답변이 섞이지 않게 한다.
  - 페이지 본문은 비신뢰 입력이며 내부 명령을 따르지 않도록 시스템 프롬프트와 길이 제한을 둔다.
- 대안: 비스트리밍 단일 응답, WebSocket, 영구 대화 저장, 이벤트별 임베딩.
- 근거와 영향: 새 인프라나 스키마 없이 현재 세션 검색과 수집 본문을 재사용하면서 첫 응답 체감과
  근거 추적성을 개선한다. 이전 답변의 오류가 다음 질문에 전파되지 않는다. extension 문서 자체를
  닫거나 새로고침하면 목록은 사라지며 브라우저 재시작 복원과 탭 간 동기화는 후속 범위다.

## 2026-08-07 — newtab 실데이터 연결은 Session → Page 2단계로 시작

- 상태: 승인
- 배경: 새 탭 홈과 Atlas는 정적 `ATLAS_ORBITS`를 사용했지만 백엔드에는 Session과
  Event만 있고 Orbit 엔티티나 Session-Orbit 관계는 없다. 기존 목업의 3단계를 실데이터처럼
  유지하려면 근거 없는 그룹을 만들거나 데이터 모델을 먼저 확장해야 했다.
- 결정:
  - 정적 Atlas 데이터는 제거하고 기존 `/sessions`, `/sessions/{id}/events`,
    `/search?scope=memory` API를 사용한다.
  - 현재 IA는 Session → Page 2단계다. Session 이름을 Atlas 중심 노드에 표시하고 Page/Event를
    `sequence_order` 순서대로 단일 궤도에 배치한다.
  - 같은 URL의 이벤트도 시간 순서 기록으로 각각 유지하되 총 방문 횟수를 각 노드에 표시한다.
  - 이벤트가 없는 snapshot 세션은 저장된 탭 순서로 보완한다.
  - Orbit → Session → Page 그룹은 백엔드 Orbit 모델과 관계 계약을 정한 뒤 고도화한다.
- 대안: 모든 Session을 임시 `나의 탐색` Orbit 아래 넣기, Session별 가상 Orbit과 내부 Session을
  중복 생성하기, 이번 작업에서 Orbit 스키마/API까지 추가하기.
- 근거와 영향: 백엔드에 없는 의미를 UI가 만들어내지 않으며, 현재 실제 데이터의 구조와 시각화
  깊이를 일치시킨다. 세션별 이벤트 조회가 N회 발생하므로 6개씩 제한해 요청하고 Query 캐시를 쓴다.

## 2026-08-07 — 제품 UI를 extension으로 단일화하고 merge UI를 사이드패널로 이전

- 상태: 승인
- 배경: 실제 서버 데이터와 연결된 extension 표면은 사이드패널이고, newtab 홈·아틀라스는 목업 데이터 기반이다.
  병합 제안·실행·되돌리기와 자동병합 토글은 독립 웹 `frontend/`에만 있어 extension 사용 흐름에서 접근할 수 없었다.
- 결정:
  - 병합 제안·개별/일괄 병합·되돌리기를 extension 사이드패널의 세션 목록으로 이전한다.
  - 자동병합 opt-in 토글은 extension 설정의 "세션 관리" 영역에 둔다.
  - 독립 웹 `frontend/`와 루트 개발 스크립트의 Vite 프로세스를 제거하고 제품 UI를 extension으로 단일화한다.
  - 웹에만 있던 상세 Analytics(반복 방문·반복 검색·일별 추이)는 종료하고, 사이드패널의 주제별 탐색 시간·최다 도메인 요약은 유지한다.
- 대안: newtab 목업을 먼저 서버에 연결해 병합과 상세 Analytics를 모두 옮긴 뒤 웹을 제거하거나, 웹을 계속 병행 운영한다.
- 근거와 영향: 현재 실사용 흐름인 사이드패널에서 데이터 변경을 검토할 수 있고 중복 UI·API 매퍼·별도 Vite 실행을 제거한다.
  newtab은 이번 결정 후에도 목업이며, 상세 Analytics를 다시 제공하려면 extension IA 안에서 별도 화면을 새로 결정해야 한다.

## 2026-08-07 — 일괄병합 도입 + 자동병합은 gated·기본 OFF

- 상태: 승인 (자동병합 실제 활성화는 사용자 결정 대기 — 현재 OFF)
- 배경: 사용자가 병합 화면에 일괄병합과 자동병합을 요청. 자동병합은 문서화된 "자동 파괴 금지" 원칙과 충돌해
  제안+위험 제시 후 결정 필요.
- 결정:
  - **일괄병합("모두 병합")** 도입 — 여전히 human-in-loop(한 번 확인). 한 배치에서 소비된 세션 재등장 시 스킵,
    성공분은 "모두 되돌리기"로 일괄 undo. 파괴 원칙과 충돌 없음.
  - **자동병합은 opt-in·기본 OFF**로만 구현. 켜져도 "명백한 중복"만: 코사인 ≥ auto_merge_floor(0.80) **AND**
    제목 토큰 자카드 ≥ 0.80(near-identical title). 전부 로그·가역. 배치 후 실행, 실패해도 배치는 성공.
  - 임의 활성화 금지 — 사용자가 명시적으로 켤 때만 동작.
  - **활성화 방식(2026-08-07 후속)**: env 플래그 대신 **UI 토글 버튼**으로 사용자가 런타임에 켜고 끈다.
    선호값은 `app_settings`(key-value) 테이블에 저장(재시작에도 유지). env `auto_merge_enabled`는 기본값, DB 값이 우선.
    새 테이블은 additive(create_all 생성)이며 단일 사용자 개인앱의 전역 설정 저장 용도.
- 대안: 자동병합 즉시 ON(원칙 위반·오병합·요약 왜곡·연쇄·신뢰 훼손 위험), 제안 floor(0.52)로 자동(사람 필터 전제가
  깨져 애매한 쌍까지 자동 병합).
- 근거와 영향: 자동병합의 "가역"은 이벤트 수준까지이며 재요약/버전 이력은 완전 원복이 아니라 반복 시 누적 왜곡 위험이
  있다. 그래서 제목까지 거의 동일한 near-duplicate로 대상을 극도로 좁히고 기본 OFF로 둔다. 실데이터 프리뷰(읽기 전용)에서
  게이트는 가비아 중복(제목 동일)만 AUTO, 제주항공권·이터널리턴·낭만인프라는 제목 상이로 수동 유지 — 의도대로 동작 확인.

## 2026-08-07 — merge P4 대시보드 UI 방식

- 상태: 승인
- 배경: P4(제안 노출)에서 UI 위치는 "대시보드 우선"으로 이미 합의. 구현 방식(제안 생성/확인 UX)만 결정 필요.
- 결정:
  - **온디맨드 조회**(새 테이블/배치 훅 없음): 대시보드가 `GET /sessions/merge-suggestions`를 로드마다 호출.
    병합 후 react-query 캐시 무효화로 재조회 → 스테일 제안(예: 한 세션이 두 제안에 등장) 자연 소거.
  - **확인 UX**: 별도 모달 없이 앱 관례(`confirm()`) + 제안 카드의 "병합" 버튼 클릭이 곧 확인. 자동 병합 없음.
  - **되돌리기 노출**: 병합 성공 토스트에 "되돌리기" 액션(6초) → `POST /unmerge`. merged 세션이 목록에서 빠져
    별도 진입점이 없으므로 토스트가 유일한 즉시 undo 경로(가역성 유지).
  - 배치 후 자동 제안 생성 + 뱃지, 익스텐션 사이드패널은 **보류**(대시보드 우선, 온디맨드로 충분).
- 대안: 제안 영속화 테이블 + 배치 훅(뱃지용이나 스키마·스테일 관리 비용), 전용 모달(앱에 모달 관례 없음).
- 근거와 영향: 최소 변경으로 대시보드에 안전히 얹고(AnalyticsSection의 "실패 시 조용히 숨김" 방어 패턴 재사용),
  가역성은 토스트 undo로 확보. 대규모(세션 수↑)에서 온디맨드 N Qdrant 질의 비용이 커지면 영속화 재검토.

## 2026-08-07 — merge P2·P3 구현 + merge_suggest_floor 튜닝 확정

- 상태: 승인 (앞 2026-08-07 P0+P1 항목의 후속 — floor 잠정값을 실측으로 확정)
- 배경: 사용자 지시로 merge 전체(P2 실행·P3 undo)를 구현하고 `merge_suggest_floor`를 실 임베딩으로 튜닝.
- 결정:
  - **P2 병합 실행**: 단일 트랜잭션 DB 이전(dedup·재계산·keyword 합집합·B soft-delete `status='merged'`),
    재요약/재임베딩·B Qdrant 포인트 삭제는 백그라운드 분리(session_updater와 동일 계층 분리). `list_sessions`는
    `status!='merged'`로 필터(archived 노출은 유지), `_fetch_candidates` 최종 조회에 `status='active'` 이중 방어.
  - **P3 undo**: `merged_from_session_id` 태그로 원 세션 복원 + 양쪽 재계산·재요약.
  - **merge_suggest_floor = 0.52** 확정(잠정 0.6 → 골든 0.56 → 실데이터 0.52로 2단계 튜닝).
    - 1단계(골든): 병합 골든셋(8쌍) 실 passage 임베딩(`eval/tune_merge_floor.py`) — 양성 0.645~0.739,
      음성 0.236~0.472 완전 분리 → 밴드 중앙값 0.56.
    - 2단계(실데이터, 채택): 도그푸딩 18세션 153쌍 읽기 전용 측정 — 실 세션 요약은 코사인이 더 낮게 분포해
      골든 0.56이면 정답을 놓침(낭만인프라↔인프라모니터링 0.533, merge-design §1 명시 케이스; 이터널리턴 강의↔나어나이 0.544).
      키워드 게이트 통과 쌍의 실데이터 분리 구간 [0.44 최상위 음성("ai"/"탐색" 광범위 겹침), 0.533 최하위 정답]
      사이 **0.52** 채택. 이 값에서 실데이터 제안 5쌍 전부 진짜 과분할(가비아 중복·제주항공권 9+3·이터널리턴 2건·낭만인프라).
- 대안: 0.56 유지(골든 기준·정밀 우선이나 실데이터 정답 낭만인프라를 놓침), 0.44 이하(광범위 단일토큰 음성 유입).
- 근거와 영향: 제안은 사용자 확인이 뒤따르고(자동 병합 아님) 키워드 AND 게이트가 교차주제를 1차 차단하므로,
  실데이터 정답을 포착하는 쪽으로 recall을 조금 더 준다. 18세션은 소표본 — 데이터가 늘면 재측정(재현: `eval/tune_merge_floor.py`
  + 실데이터 읽기 전용 스크립트). SQL 경로는 일회용 postgres 컨테이너 통합 검증으로 실제 실행 확인.

## 2026-08-07 — 세션 병합(merge) P0+P1: 범위·탐지 신호·생존자·UI 위치

- 상태: 승인
- 배경: 서브클러스터링/게이팅으로 메가 뭉침은 해소했으나 반대 결함(같은 주제 과분할·중복 세션)이 남았다.
  merge-design.md는 설계만 있었고 §9 열린 결정(임계값·생존 선택·노출 시점·UI)이 미합의 상태였다.
- 결정:
  - 범위 = **P0(스키마) + P1(읽기 전용 제안 API)**만 착수. P2(실행)·P3(undo)·P4(자동 노출)는 별도 승인.
  - 후보 탐지 = **벡터 유사도 >= merge_suggest_floor AND 키워드/제목 토큰 겹침**(정밀 우선, 오탐 억제).
  - 생존(흡수) 세션 = **이벤트 많은 쪽**, 동률이면 이른 started_at(→created_at), 그다음 작은 id(결정적).
  - 제안 UI 위치 = **웹 대시보드 우선**(검토 화면 성격). UI 구현 자체는 P2 범위.
  - 소프트삭제+undo(가역), 자동 병합 금지는 merge-design 확정안 유지.
- 대안: 벡터만 탐지(재현율↑·오탐↑), 생존자=먼저 만든 세션(요약 편향 위험), 사이드패널 UI(다중 후보 비교 불편).
- 근거와 영향: 제안이라도 오탐은 신뢰를 깎으므로 AND 조건으로 정밀 우선. P0에서 `sessions.merged_into`만
  추가하고 `session_events.merged_from_session_id`는 실제 이벤트 이전이 있는 P2로 연기(투기적 스키마 지양).
  `merge_suggest_floor` 기본 0.6은 미측정 잠정값 — 골든/실데이터 튜닝 후 본 항목을 후속으로 갱신한다.
  **P2 필수 조건**: `list_sessions`에 status 필터가 없어 merged 세션이 목록에 노출되므로 P2에서 필터 추가 필요.

## 2026-07-05 — 탭 클러스터링에 LLM 유지

- 상태: 승인
- 배경: 짧은 제목과 URL, 수십 개 이하의 표본에서는 임베딩 기반 밀도 클러스터링의 품질과 안정성이 낮았다.
- 결정: Solar Mini 기반 프롬프트 클러스터링을 유지한다. 임베딩은 세션 검색에 사용한다.
- 대안: HDBSCAN과 cosine similarity 기반 클러스터링
- 근거와 영향: LLM이 그룹핑과 주제명 생성을 함께 수행할 수 있다. 외부 응답 실패와 잘못된 구조를 검증하고 fallback해야 한다.

## 2026-07-05 — 요약과 검색 모델 역할 분리

- 상태: 승인
- 배경: 작업 복잡도와 장애 특성이 서로 다르다.
- 결정: 요약은 A.X-K1을 우선 사용하고 Solar Pro 3를 fallback으로 사용한다. 경량 클러스터링과 리랭킹은 Solar Mini를 사용한다. 저장 벡터는 passage 모델, 검색어는 query 모델을 사용한다.
- 영향: 모델별 오류와 제한을 서비스 계층에서 구분해 처리해야 한다.

## 2026-07-05 — 민감 페이지는 본문만 제외

- 상태: 승인, 도메인 목록 보완 필요
- 배경: 개인정보 전송을 줄이면서 탭 복원 기능은 유지해야 한다.
- 결정: 민감 URL에 해당하면 제목과 URL은 유지하고 `text_content`와 `excerpt`는 비운다.
- 영향: 민감 도메인 판정의 정확성과 최소 권한 검토가 필요하다.

## 2026-07-12 — AI 요약 fallback과 실패 상태 분리

- 상태: 승인
- 배경: LLM 실패를 규칙 기반 요약으로 바꾸면 `summary_status=done`으로 기록되어 재시도와 장애 관측이 불가능했다.
- 결정: 규칙 기반 제목과 임시 요약은 최초 저장 응답에만 사용한다. AI 요약 오류와 빈 overview는 실패로 기록하고 임베딩하지 않는다.
- 영향: 기존 실패 재시도 API와 UI가 실제 외부 AI 실패 경로에서 동작한다.

## 2026-07-12 — 민감 도메인은 명시 목록으로 제한

- 상태: 승인
- 배경: `.or.kr` 전체 차단은 무관한 학회와 협회까지 제외했고 실제 국내 금융 도메인은 누락했다.
- 결정: 일반 `.or.kr` 차단을 제거하고 금융, 결제, 정부, 의료 도메인을 명시적으로 관리한다. 미사용 `bookmarks` 권한을 제거한다.
- 영향: 개인정보 보호 범위와 요약 품질의 균형이 개선되며 도메인 목록의 지속적인 검증이 필요하다.

## 2026-07-12 — 검색 score threshold를 설정으로 관리

- 상태: 승인, 실측 튜닝 대기
- 배경: Qdrant가 유사도가 낮은 결과도 항상 top-N으로 반환해 무관한 세션이 노출될 수 있었다.
- 결정: cosine score threshold 기본값을 `0.35`로 적용하고 `SEARCH_SCORE_THRESHOLD` 환경변수로 조정한다. 저장 임베딩에는 세션 제목을 포함한다.
- 대안: 고정 상수 사용, threshold 없이 리랭커만 사용
- 영향: 무관한 결과를 줄일 수 있지만 실제 골든셋에 따라 누락률이 증가할 수 있다. 기존 포인트는 별도 재색인이 필요하다.

## 2026-08-03 — 제품 방향 전환: 탭 스냅샷 분류 → 상시 이벤트 수집 + 배치 세션화

- 상태: 승인
- 배경: 열린 탭을 분류·요약해 저장하는 것 자체는 지속적인 사용자 가치가 아니며, 탐색 중 사고의 흐름(Context)이 시간이 지나면 사라지는 문제가 더 근본적이다. 기존 요약(`generate_summary`), 임베딩 검색(Qdrant), 복원, 민감 도메인 필터 파이프라인은 이미 검증되어 재사용 가능하다.
- 결정: Orbit을 "탭 스냅샷 분류" 도구에서 "상시 이벤트 수집 + 배치 세션화" 기반 Personal Exploration Memory로 전환한다. 방문 이벤트마다 LLM을 호출하지 않고, 로컬 큐에 모은 이벤트를 동기화 트리거(수동/주기/개수/유휴) 시점에 배치로 분석해 세션을 자동 생성·갱신한다(Auto Session). 세션 분류 단위는 고정 규칙이 아니라 배치마다 LLM 의도 분석이 이벤트 그룹별로 append(기존 세션에 추가)·create(신규 세션 생성)·hold(보류)·discard(제외) 중 하나로 판단한다. 세션 간 자동 병합(merge)은 후순위로 미루고 스키마만 예약한다.
- 대안: 기존 탭 스냅샷 분류 방식 유지, 전면 리라이트(기존 파이프라인 폐기 후 처음부터 재설계)
- 근거와 영향: 탭 관리 자체는 사용자 가치가 아니라는 판단과, 기존 파이프라인을 최대한 재사용할 수 있다는 점에서 점진적 확장을 선택했다. 데이터 모델에 이벤트 계층(`exploration_events` 등)이 추가되고, Extension에 신규 권한(webNavigation 등)이 필요하다.

## 2026-08-03 — DB 마이그레이션: 1회 리셋 + 멱등 ALTER 러너

- 상태: 승인
- 배경: Alembic 등 정식 마이그레이션 도구 없이 `create_all`만 사용하고 있었고, 이벤트/세션 관련 신규 테이블·컬럼 추가가 필요했다.
- 결정: 1회 `docker compose down -v` 리셋 + `create_all` + 멱등 ALTER 러너(`app/db/migrations.py`, ~25줄)를 도입한다. Alembic 도입은 대회 이후로 미룬다.
- 대안: Alembic 즉시 도입, `create_all`만 유지(스키마 변경마다 반복 리셋 필요)
- 근거와 영향: 대회 일정 내 최소 비용으로 스키마 변경을 반영하기 위한 임시 조치다. 로컬/데모 데이터는 1회 소실되며, 이후 안정화 단계에서 Alembic 전환이 필요하다.

## 2026-08-03 — 방문 감지: webNavigation 중심, history 권한 미사용

- 상태: 승인
- 배경: 상시 이벤트 수집을 위해 브라우저 방문을 감지할 방법이 필요했다. `history` 권한은 설치 시 새로운 경고를 유발할 수 있고, `chrome.history.onVisited`는 tabId/windowId/리다이렉트 판별 정보가 부족하다.
- 결정: `webNavigation.onCommitted`(frameId 0 = 방문 원천) + `onHistoryStateUpdated`(SPA 내비게이션) + `tabs.onUpdated`(제목 보강)를 중심으로 방문을 감지한다. `history` 권한은 사용하지 않는다(과거 기록 백필은 추후 opt-in 옵션으로만 검토).
- 대안: `history` 권한 + `chrome.history.onVisited` 사용
- 근거와 영향: `webNavigation` 권한의 설치 경고는 기존 `tabs` 권한과 동일한 수준이라 사용자에게 새로운 경고가 노출되지 않는다. `onCommitted`/`onHistoryStateUpdated`는 tabId/windowId/리다이렉트 여부가 이벤트에 포함되어 있어 체류시간 세그먼트·리다이렉트 치환 등 상관관계 계산에 유리하다. 새 경고를 유발하지 않는 권한만 사용하기로 해 최소 권한 원칙과 조화시켰다.

## 2026-08-03 — 로컬 큐: IndexedDB + idb

- 상태: 승인
- 배경: 방문 이벤트를 실시간으로 로컬에 쌓고 SW 종료/재시작에도 유실 없이 보존해야 한다.
- 결정: IndexedDB + `idb`(~1.2KB, 신규 의존성 1개)로 로컬 Persistent Queue를 구현한다. 사이드패널이 같은 DB를 SW를 깨우지 않고 직접 열람할 수 있다.
- 대안: `chrome.storage.local`
- 근거와 영향: `chrome.storage.local`은 read-modify-write 경합(동시 쓰기 시 덮어쓰기 위험)이 있어 이벤트 스트림처럼 빈번한 append 작업에 부적합하다. IndexedDB는 트랜잭션 기반이라 유실·경합 위험이 낮고, 사이드패널이 큐를 직접 읽어 미동기화 이벤트도 즉시 렌더링할 수 있다.

## 2026-08-03 — 세션 요약 저장: summary JSONB 유지, session_versions만 개별 컬럼

- 상태: 승인
- 배경: 새 방향 초안은 세션 요약 필드를 개별 컬럼으로 승격하는 안을 제시했으나, 기존 프론트/검색/임베딩 코드는 `summary` JSONB 구조에 맞춰져 있다.
- 결정: `summary` JSONB 구조를 그대로 유지한다. 버전 이력만 `session_versions` 테이블에 개별 컬럼(title/overview/purpose/highlights/todos/next_actions 등)으로 저장한다.
- 대안: 스펙대로 `summary` 필드를 개별 컬럼으로 승격
- 근거와 영향: 개별 컬럼 승격은 기존 프론트엔드 타입, 검색 임베딩 입력 조합, 요약 파싱 로직을 모두 바꿔야 해 호환 비용이 크다. JSONB를 유지하면 기존 코드가 무변경으로 동작하고, 버전 이력만 별도 테이블로 분리해 이력 조회 요구를 충족한다.

## 2026-08-03 — 배치 실행: in-process asyncio + asyncio.Lock

- 상태: 승인
- 배경: 배치 세션화 파이프라인은 동시 실행 방지와 재시작 안전성이 필요하지만, 대회 일정상 신규 인프라(큐/워커) 도입은 과도하다.
- 결정: in-process `asyncio` + 모듈 레벨 `asyncio.Lock`으로 배치를 직렬화한다. `sync_batches.status='running'` 행을 재시작 안전망으로 사용하고, 시작 시 `running→failed`, `processing→pending`으로 복구한다(기존 `recover_pending_sessions` 확장). Redis나 별도 워커는 도입하지 않는다.
- 대안: Celery, ARQ 등 별도 워커/큐 인프라 도입
- 근거와 영향: 현재 서비스 규모와 대회 일정에서는 별도 인프라 없이도 단일 프로세스 락과 DB 상태 행으로 동시 실행 방지와 재시작 복구를 충분히 처리할 수 있다. 향후 다중 인스턴스 배포 시에는 분산 락으로 전환이 필요하다.

## 2026-08-05 — Extension 단위 테스트: vitest + fake-indexeddb 도입

- 상태: 승인 (사용자 승인, 2026-08-05)
- 배경: `lib/events`·`lib/sync`는 M2 완료 게이트 리뷰에서 단위 테스트 부재가 지적됐으나, 테스트 프레임워크 도입이 신규 dev 의존성이라 사용자 결정 대기로 보류돼 있었다.
- 결정: `vitest` + `fake-indexeddb`를 devDependency로 추가한다. WxtVitest 플러그인 없이 plain vitest 구성으로 `wxt/testing`의 `fakeBrowser`만 chrome.* 전역 대역으로 사용한다. 검증 명령은 `pnpm test`.
- 대안: WxtVitest 플러그인 전체 구성(자동 import·vite 플러그인 체인 포함), jest
- 근거와 영향: lib 코드는 wxt 모듈이 아닌 chrome 전역과 IndexedDB 전역을 직접 사용하므로 WXT 플러그인 기계 없이 전역 대역 2개(fakeBrowser, fake-indexeddb)로 충분하다. 구성이 단순할수록 WXT 버전 업그레이드와 독립적이다. E2E는 Playwright(MCP 등록 완료)로 별도 수행한다.

## 2026-08-05 — 의도 분석 프롬프트 v2 + temperature 0

- 상태: 승인
- 배경: 평가 하네스에서 노이즈 제외율이 50%에 고정 — 짧은 SNS 스침 방문을 discard하지 않고 별도 세션으로 create했다. 또한 temperature 미지정(기본 0.3)으로 실행마다 판정이 흔들려 평가 재현성이 낮았다.
- 결정: `intent_analyzer.py` 프롬프트를 v2로 올린다 — ① discard 기준에 "체류 약 1분 미만 + 어떤 주제와도 무관한 스침 방문(SNS 피드·쇼츠/릴스·포털 홈)" 명시 ② 도구성 방문(번역기·지도 등)이 주제 흐름에 속하면 보호 ③ 서로 다른 주제는 assignment 분리 강제 ④ JSON 예시를 다중 assignment(append+create+discard)로 확장. LLM 호출에 `temperature=0.0`을 명시한다.
- 대안: 체류시간 기반 서버측 사전 필터(LLM 무호출 discard 규칙), temperature 유지(0.3)
- 근거와 영향: temperature 0 단독은 "전부 한 세션으로 뭉치는" 나쁜 greedy 경로에 결정적으로 고정됐고(3회 동일: coverage 0.75, noise 50%), 다중 assignment 예시 앵커링과 주제 분리 지침을 더하자 greedy 경로가 교정됐다(3회 중 2회 전 지표 100%, 1회 noise 50% — API 측 잔여 비결정성). 결정적 사전 필터(예: 30초 미만 SNS 도메인 즉시 discard)는 데이터 처리 정책 변경이므로 별도 사용자 결정 항목으로 남긴다.

## 2026-08-05 — 검색 score threshold 0.35 → 0.28 (실측 튜닝)

- 상태: 승인
- 배경: 2026-07-12 결정에서 0.35는 "초기 기본값이며 골든셋 실측 후 조정" 조건부였다. 검색 골든셋(`eval/golden_retrieval.json`: 세션 10, 긍정 질의 14, 음성 질의 5)과 평가 스크립트(`eval/run_retrieval_eval.py`)를 만들어 실측했다.
- 결정: `search_score_threshold` 기본값을 0.28로 낮춘다.
- 대안: 0.35 유지(긍정 질의 2개 유실 — "리트코드 문제 풀던 세션" 0.289, "일본 항공권·호텔" 0.320), 0.25(음성 질의 1개 유입 — "헬스장 PT" 0.265→laptop_shopping), 0.30(긍정 1개 유실)
- 근거와 영향: 음성 질의 최고점(0.2648)과 정답 최저점(0.2888) 사이에 깨끗한 분리 구간이 있어 그 중앙(0.28)에서 이 골든셋 기준 Recall@1 100% + 음성 차단 100%를 동시에 달성한다. 분리 폭이 0.024로 좁으므로 골든셋을 더 확대하면 재검증이 필요하다. 이 threshold는 `/search`와 sync_pipeline의 후보 세션 검색이 공유한다 — 후보 검색에는 관련 세션이 조금 더 넓게 잡히는 방향(완화)이라 append 판단은 LLM이 최종 결정하므로 위험이 낮다.

## 2026-08-05 — LLM 재배정: 클러스터링=EXAONE, 요약·의도분석·리랭킹·채팅=A.X (상호 폴백)

- 상태: 승인 (사용자 자체 평가 결과 성능·비용 최적 조합)
- 배경: 기존 배선은 A.X-K1(요약·의도분석) + Solar Mini(클러스터링·리랭킹) + Solar Pro 3(공용 폴백)였다.
- 결정: `chat_completion_light` = EXAONE(FriendliAI dedicated endpoint) 우선 → A.X-K1 폴백(클러스터링). `chat_completion(_with_meta)` = A.X-K1 우선 → EXAONE 폴백(요약·의도분석·리랭킹·향후 채팅). 리랭커는 light → chat_completion으로 전환(temperature 0.1 유지). Solar 채팅 모델 설정 제거, Upstage는 임베딩 전용으로 유지. 의도분석은 A.X 유지(프롬프트 v2가 A.X 기준으로 튜닝·평가됨).
- 대안: Solar Pro 3 폴백 유지(3사 의존), 폴백 없음
- 근거와 영향: 사용자 평가에서 EXAONE(클러스터링)/A.X(요약·리랭킹) 조합이 성능·비용 최적. 상호 폴백으로 채팅 경로의 Upstage 의존 제거. EXAONE 4.0은 hybrid reasoning 모델이라 `chat_template_kwargs.enable_thinking=false`를 항상 전달해야 content가 채워진다(실측).
- **미해결 리스크**: 현재 FriendliAI dedicated endpoint가 웜 상태에서도 호출당 ~60초(4회 연속 실측)로, 25초 타임아웃에 걸려 클러스터링이 항상 A.X로 폴백된다. 엔드포인트 사양/상태 확인 전까지 EXAONE은 사실상 대기 상태 — FriendliAI 콘솔 확인 필요(열린 결정 표 참조).

## 2026-08-05 — 의도분석 모델 재확인: A.X 유지 (EXAONE 비교, 정정 포함)

- 상태: 확정 (기존 "의도분석 A.X 유지" 결정 유지)
- 배경: "분류는 EXAONE이 해야 하지 않나"라는 재질문에, 배치 의도분석(append/create/hold/discard)을 A.X→EXAONE으로 바꿀 경우를 골든셋으로 비교했다. 1차 자동 비교(각 3회) 후 사용자 요청으로 두 모델의 **원본 응답을 직접 캡처**해 형식을 검증했다.
- **정정 1 — JSON 형식**: 1차에서 "EXAONE이 JSON 형식을 못 따른다"고 서술했으나 **오류였다.** 원본 캡처 결과 EXAONE의 JSON은 9개 시나리오 전부 문법·스키마 정상(예외 1건: shopping에서 이벤트 1개 미할당 → hold fallback). 형식 문제 아님.
- **정정 2 — 32.5% 수치 철회**: 1차 자동 비교의 EXAONE 배정 정확도 32.5%·커버리지 33%는 **신뢰 불가**. EXAONE serverless가 rate limit(429)을 자주 반환했고, 재시도 실패 시 analyze()가 그룹 전체를 hold 처리해 커버리지가 인위적으로 붕괴했다 — 모델 품질이 아니라 rate limit을 상당 부분 측정한 값.
- 실제 관찰된 차이(원본 캡처 기준): ① 다주제 그룹에서 EXAONE 과분할(여행+코딩 2주제를 A.X는 2세션, EXAONE은 4세션) ② EXAONE은 title·purpose를 자주 빈 문자열로 둠 ③ 단순·단일주제 그룹은 두 모델 사실상 동등(EXAONE이 더 간결하기도).
- **정정 3 — 공정 재평가 결과(EXAONE 튜닝 프롬프트 + rate limit 제어)**: "EXAONE에 맞게 튜닝하면 잘 나오지 않나"라는 사용자 가설을 검증했다. EXAONE 전용 프롬프트(과분할 억제 + WRONG/RIGHT 예시 + title·purpose 강제)를 만들고, 429를 백오프 재시도로 흡수해 hold 오염을 제거한 뒤 각 모델을 자기 최선 프롬프트로 3회씩 비교.
  - 결과: 배정 정확도 A.X 100% vs **EXAONE 97.4%**, 커버리지·순도 동률(100%), new/existing 100% vs 96.3%, **노이즈 제외 A.X 94.9% vs EXAONE 100%(EXAONE 우세)**, 실패 0.7 vs 1.0. → 품질 사실상 대등.
  - 과분할은 튜닝으로 해소됨(mixed_topics: 튜닝 전 여행/코딩을 4개로 쪼갬 → 튜닝 후 각 1개로 정확히 묶고 title·purpose 채움).
  - 즉 앞서의 "32%"·"A.X가 더 낫다"는 프롬프트 불공정 + rate limit 오염 탓이었고, 사용자 가설이 옳았다.
- 결론(수정): **품질은 두 모델을 가르지 못한다.** 배치 의도분석의 모델 선택은 이제 ① 비용(EXAONE 저렴) ② serverless rate limit 운영 가능성 두 축의 결정이다. rate limit이 관건 — 재평가 내내 429가 반복돼 실제 배치(그룹 연속 처리)에서 리스크. 후보 경로: EXAONE primary→A.X fallback(429 시 A.X가 받음) 하이브리드.
- 남은 검증: 골든셋 과적합 가능성(패턴 알고 튜닝함) → held-out/실데이터 검증 필요. rate limit은 실제 배치 처리량으로 별도 검증.
- 참고: 상시 수집→자동 세션화 경로는 EXAONE 클러스터러를 쓰지 않는다(시간 그룹핑 + 의도분석). EXAONE 클러스터링(cluster_tabs)은 수동 스냅샷 경로 전용.

## 2026-08-05 — 의도분석 EXAONE-primary 하이브리드 전환 (구현·실데이터 검증 완료)

- 상태: 승인·구현 (사용자 결정: "EXAONE 메인, 오래 대기 시 A.X fallback, 실데이터 검증")
- 결정: 배치 의도분석을 **EXAONE primary → A.X-K1 fallback**으로 전환. `llm.chat_completion_intent` 신설(EXAONE 우선, `max_retries=0`+`timeout=12s`로 429/지연 시 즉시 A.X 폴백 — 백오프로 오래 대기하지 않음). 프롬프트는 공정 재평가에서 EXAONE 과분할을 잡은 튜닝판을 **v4로 프로덕션 승격**(과분할 금지 블록 + title·purpose 강제). 요약·리랭킹용 `chat_completion_with_meta`(A.X primary)는 무변경.
- 근거: 공정 재평가에서 품질 대등(EXAONE 배정 97.4%, 노이즈 제외 EXAONE 우세) + EXAONE 저비용. rate limit은 하이브리드로 흡수.
- 실데이터 검증(139개 이벤트 재세션화): EXAONE 폴백 3건만 발생(대부분 EXAONE 처리, 폴백 즉시·배치 오류 0). 주제 뭉침 대폭 개선("여름음악+맥미니+브랜딩" → 3개로 분리). 잔여 한계: flight+mail 류 일부 뭉침 남음. v4 골든셋 무회귀(100%).
- 남은 관측: 골든셋 과적합 가능성은 실데이터에서 대체로 해소.

## 2026-08-05 — 프롬프트 v5(일반 메일 분리) + 배치 감사 필드 개선

- 상태: 승인·구현·실데이터 검증 완료
- 배경: 하이브리드 전환 후 실데이터에 flight+mail 잔여 뭉침. 원인은 그룹 간 과잉 append(2.5시간 뒤 메일 확인 그룹이 항공권 세션 후보에 append됨). 또 `SyncBatch.model` 감사가 임의 1개만 기록해 EXAONE/A.X 폴백률이 안 보였음.
- 결정: ① 프롬프트 v5 — "일반 메일 확인(받은편지함)은 명확한 확인 메일이 아니면 무관한 세션에 append 금지, 별도 '메일 확인' 세션 또는 discard". ② `SyncBatch.model`을 그룹별 사용 모델 카운트 요약으로 기록(`_summarize_models`, 예 `exaone:3,A.X-K1:1`) — 스키마 변경 없이 기존 String(50) 재사용(Alembic 부재 대응).
- 검증: 골든셋 10개 전 지표 100%·실패 0(무회귀), 신규 메일 시나리오 100%. 실데이터 139개 재세션화에서 flight/mail 완전 분리(항공권 2세션·나망메일·네이버메일·gmail·github 각각 독립), 감사 필드 `exaone:3,A.X-K1:1`로 폴백률 관측.
- 남은 한계(경미): 시간 버스트로 항공권 2세션 분리(둘 다 flight), 습관성 짧은 inbox 스침의 세션화 vs discard 경계는 노이즈 필터 확장 후보.

## 2026-08-05 — EXAONE dedicated → serverless 전환

- 상태: 승인
- 배경: dedicated endpoint가 웜 상태에서도 호출당 54~70초(첫 바이트까지 55초 — 전부 서버측 지연, 네트워크 정상)로 25초 타임아웃에 항상 걸려 클러스터링이 A.X로만 폴백되고 있었다.
- 결정: FriendliAI serverless `LGAI-EXAONE/K-EXAONE-236B-A23B`로 전환(base_url/model 기본값 변경). 종량제 $0.2/$0.8 per M tokens.
- 대안: dedicated 인스턴스 사양 상향(콘솔 확인·비용 증가), 클러스터링 A.X 회귀(모델 다양성 상실)
- 근거와 영향: 실측 0.3~0.5초/호출로 170배 빠르고 실제 코드 경로(chat_completion_light) 스모크 통과. A.X 다운 시 EXAONE 폴백도 3.3초에 성공. 주의: serverless는 버스트에 민감한 rate limit이 있어(테스트 중 연속 호출로 429 1회 관찰, 45초 후 회복) 429 시 A.X 폴백(light 경로) 또는 오류 전파(with_meta 폴백 경로)로 처리된다 — 현행 0.5초 전역 리미터로 통상 트래픽에서는 문제없을 것으로 판단.

## 2026-08-05 — 세션 목록 정렬·표시 기준: created_at → last_activity_at

- 상태: 승인 (도그푸딩 1차 피드백)
- 배경: append로 탭이 추가된 세션이 목록에서 옛 `created_at`으로 표시·정렬되어 아래에 묻혔다(사용자 보고). `last_activity_at`은 append 시 정상 갱신되고 있었으나 정렬·표시에 쓰이지 않았다.
- 결정: `GET /sessions` 정렬 키를 `coalesce(last_activity_at, created_at) desc`로 변경하고 응답에 `last_activity_at`(nullable)을 추가한다. 사이드패널·웹 대시보드의 timeLabel도 `last_activity_at ?? created_at` 기준으로 바꾸고 상세 화면 라벨을 "저장"→"활동"으로 조정한다.
- 대안: created_at 유지 + 별도 "최근 활동" 배지, updated_at 기준 정렬(재요약만 해도 튀어오르는 부작용)
- 근거와 영향: Auto Session은 스스로 성장하는 세션이므로 사용자 기억과 맞는 시간축은 마지막 활동이다. snapshot 세션(last_activity_at 없음)은 created_at으로 동작이 불변. 같은 작업에서 `DELETE /sessions/{id}`가 자식 행(session_events·session_versions)을 지우지 않아 FK 오류가 나던 잠재 결함도 수정했다(애플리케이션 계층 정리 — FK ON DELETE 추가는 Alembic 부재로 보류).

## 2026-08-05 — 세션화 후보 recency 컷 7일 + 후보 프롬프트에 최근 활동 표기 (프롬프트 v3)

- 상태: 승인 (도그푸딩 1차 피드백)
- 배경: 벡터 유사 후보에 시간 제한이 없어 한 달 전(7/3) 테스트 세션이 append 대상으로 올라갔고, LLM은 후보의 나이를 알 수 없어 그대로 append했다.
- 결정: `_fetch_candidates`에서 모든 후보에 `coalesce(last_activity_at, created_at) >= now-7일` 컷을 적용하고(최근 활동 후보는 기존 24h 컷이라 항상 통과), 후보 프롬프트 라인에 "마지막 활동: 오늘/N일 전"을 표기한다(`last_activity_days_ago`, 없으면 생략 — 골든셋 하위호환). PROMPT_VERSION v2→v3.
- 대안: 컷 없이 프롬프트 표기만(LLM 변동성에 의존), 30일 컷(한 달 전 재개 탐색 허용)
- 근거와 영향: "일주일 넘게 활동 없던 탐색은 이어지는 탐색이 아니라 새 탐색"이라는 제품 판단을 결정적 규칙으로 강제한다. 오래전 주제를 다시 탐색하면 새 세션이 생기고, 병합은 후순위 merge 기능의 몫으로 남긴다.

## 2026-08-05 — 의도 분석 프롬프트 추가 보강 반려 (A.X-K1 비결정성 실측)

- 상태: 반려 (2건 시도 후 원복)
- 배경: 실데이터에서 스침 방문(대학 포털 로그인 3초, Kaggle 홈 29초)이 세션에 혼입됐다. 실데이터 골든 시나리오 2개를 추가하고 프롬프트 보강을 시도했다.
- 시도와 결과: ① discard 예시 확대("사이트 홈·로그인 화면 스침") → job_search 정확도 100%→40% ② "무관 스침 방문을 event_indices에 끼워넣기 금지" 지침 → 동일 회귀 2회 재현. 둘 다 원복.
- 발견: A.X-K1은 temperature 0에서도 실행 간 판정이 흔들린다(같은 프롬프트로 전체 골든셋 100% ↔ 실패 2건). 배정 정확도는 97~100%로 안정적이나 초 단위 체류의 경계 이벤트는 discard/hold/혼입 사이를 오간다. hold 누적은 discard가 아니라 강제 create로 빠지므로(_HOLD_FORCE_CREATE_THRESHOLD) 스침 방문이 잡동사니 세션으로 승격될 수 있다.
- 결론: 프롬프트 미세조정으로는 해결 불가 — 결정적 노이즈 사전 필터(열린 결정)의 우선순위를 상향한다. 후보군: 체류 임계 미만 + 검색어 없음 + 그룹 내 고립 도메인 조합 규칙, 또는 self-consistency(다수결) 판정.

## 2026-08-05 — 노이즈 사전 필터 도입 (결정적 규칙, LLM 호출 전) + discarded 이벤트 Timeline 노출

- 상태: 승인·구현 (사용자 승인 2026-08-05)
- 배경: A.X-K1 비결정성으로 프롬프트 보강이 반려된 뒤(위 "프롬프트 추가 보강 반려"), 스침 방문 혼입을 결정적 규칙으로 해결하기로 했다. 실데이터에서 의미 있는 탐색도 5~42초였으므로 체류시간 단독 컷은 탐색 본체를 날린다는 제약이 핵심이었다.
- 결정: `noise_filter.py`(순수 함수)를 새로 두고 `sync_pipeline._process_group`에서 LLM 호출 전 적용한다. **구제 조건 우선**(검색어 존재 / 그룹 내 같은 도메인 2건+ / 체류 미측정이면 무조건 LLM에 전달), 그 다음 discard 규칙 3종: ① 인증·로그인 경로(login/signin/sso/auth/2fa/logout) && <60초 ② 습관성 도메인(instagram·x·facebook·tiktok·youtube 홈/쇼츠·naver·daum·nate·zum 루트) && <60초 ③ 고립 루트(`/`) && ≤30초 && 그룹 내 유일 도메인. 걸린 이벤트는 삭제가 아니라 `sync_status='discarded'`. hold 상한 정책도 변경: 상한 도달 시 짧고(<60초) 검색어 없는 스침은 강제 create 대신 discard(잡동사니 세션 승격 방지).
- Timeline 노출: `GET /events?date=`가 discarded 이벤트도 반환하고 `excluded=true` 필드를 추가한다(사용자 결정 — 수집은 다 보이되 세션에서만 제외). 사이드패널 Timeline은 "제외됨" muted 뱃지로 표시한다.
- 대안: LLM self-consistency 다수결(비용 3배, 여전히 확률적), 임베딩 유사도 필터(임계값 튜닝 추가), 확장에서 수집 자체 차단(Timeline raw 기록 가치 상실 — 반대)
- 근거와 영향: 골든셋 9개 평가에서 노이즈 제외율이 ~69%(변동) → 100%(결정적)로 올라가고 배정 정확도·purity 100% 고정. 실데이터 5건(한밭대 로그인 3s, Kaggle 홈 29s)이 LLM 무호출로 discard되고 항공권 이벤트(검색어·도메인 반복)는 구제된다. 남은 new/existing 변동(append vs create)은 이 필터 범위 밖의 LLM 판단이라 별개.
- 잔여 리스크: 습관성 도메인 목록은 하드코딩 8종으로 시작 — 확장 필요 시 설정 분리. 임계값(60s/30s)은 event_filter의 민감 도메인 목록처럼 클라이언트(sensitive-domains.ts)와는 무관한 서버 전용.

## 2026-08-05 — AI 챗 대화 URL 정규화 + 진입 화면 노이즈 규칙

- 상태: 승인·구현 (도그푸딩 2차 피드백)
- 배경: ChatGPT/Claude 대화가 SPA nav로 같은 대화(`/c/<id>`)를 여러 이벤트로 수집하고, 배치 경계 탓에 dedup을 못 거쳐 한 대화가 여러 세션으로 파편화됐다. 노이즈 필터의 도메인 반복 구제가 `chatgpt.com/` 진입 화면을 살렸다.
- 결정: `normalize_url`이 AI 챗 도메인(chatgpt.com·chat.openai.com·claude.ai·gemini.google.com)의 query를 통째로 제거해 같은 대화를 한 URL로 접는다. 노이즈 필터에 "AI 챗 진입 화면(대화 id 없는 루트·/new) && <60초"를 도메인 반복 구제보다 우선하는 규칙으로 추가한다.
- 근거와 영향: 재세션화에서 대화 5→1·6→1 dedup, chatgpt.com/ discard 확인. 정규화는 서버 전용이라 클라이언트 무변경. 실질 파편화 해소는 정규화보다 "전체 pending→단일 배치"에서 dedup이 동작한 덕이 컸다(같은 대화들이 이미 동일 normalized_url이었으나 배치가 갈려 못 합쳐졌던 것).

## 2026-08-05 — [열린 문제] LLM 주제 뭉침 (시간 인접 이질 주제)

- 상태: 미해결 (도그푸딩 2차에서 표면화, 결정 필요)
- 문제: 노이즈·파편화를 걷어내도 LLM(A.X-K1)이 시간 인접한 서로 다른 주제를 한 세션에 뭉친다. 실데이터에서 "여름 음악 + 맥미니 구매 + 브랜딩 로고 + 명함"이 한 세션, "네이버 메일 + Claude 로고 대화 + 옛 위키"가 한 세션으로 묶였다. 그룹 내 다주제 분리 실패 + 그룹 간 과도한 append가 겹친 결과.
- 노이즈 사전 필터·프롬프트 튜닝으로는 해결 안 됨(주제 분리는 본질적으로 의미 판단이고 A.X 변동성이 큼).
- 후보 방향(결정 필요): ① 그룹 내 임베딩 서브클러스터링으로 다주제를 먼저 쪼갠 뒤 클러스터별 LLM 판정 ② 그룹 간 append 게이팅 강화(벡터 유사도 하한 + 시간 근접 요구) ③ LLM self-consistency(다수결) ④ 그룹 시간 gap 축소(30분→15분).

## 2026-08-05 — 메일: 실제 읽은 메일만 기억(목록 보기 discard) [C안]

- 상태: 승인·구현·검증 완료
- 결정(사용자): 받은편지함·폴더 목록 새로고침은 탐색 기억 대상 아님 → 노이즈 필터가 결정적으로 discard. 개별 메일 읽기(/message/<id> 등)는 보존해 LLM 판단. 하이브리드(C안).
- 구현: `noise_filter._is_mail_list_view` — 웹메일 호스트(mail.*/outlook.*)의 목록/폴더 경로는 체류·도메인 반복 무관 discard, 개별 메일 읽기 경로는 제외(보존). gmail은 정규화 후 경로 구분 불가라 규칙에서 자연히 빠져 보존(한계, 명시).
- 검증: 골든 11개 무회귀(신규 mail_inbox_refresh noise 100%, mail_browsing 세션화 100%), 실데이터에서 나망 받은편지함 잡동사니 세션 소멸.

## 2026-08-06 — 그룹 내 서브클러스터링 + append 게이팅 구현 (1+2, 골든 검증 완료)

- 상태: 승인·구현·골든 검증 완료 (사용자 결정: 1+2, 조건부 하드 스플릿)
- 배경: 아래 "[재확인] 그룹 간 과잉 append"의 구조적 해법. 프롬프트 v4·v5는 특정 케이스만 잡고
  run별 불안정을 못 잡음이 확인됨.
- 결정:
  - **① 그룹 내 임베딩 서브클러스터링**(`subclusterer.subcluster`, 순수 함수, average-linkage 응집).
    30분 시간 그룹을 LLM 전에 주제별로 선분리한다. **조건부 하드 스플릿** — 클러스터가 2개 이상일
    때만 클러스터별로 후보검색+LLM을 분리(단일주제 그룹은 1회 호출로 기존과 동일). 이벤트별 임베딩은
    `embed_many`로 1회 배치 요청, centroid를 클러스터 후보검색 벡터로 재사용. **collect-then-apply**로
    클러스터가 서로의 새 세션을 후보로 보지 못하게 해 쪼갠 주제 재병합을 막는다.
  - **② append 게이팅**(`sync_pipeline._gate_appends`, 순수 함수). LLM이 append를 택해도
    후보의 벡터 유사도가 `append_score_floor`(기본 0.35) 미만이거나 마지막 활동이
    `append_max_age_days`(기본 3일) 초과면 create로 강등. `_fetch_candidates`가 버리던 벡터 score를
    후보 dict에 노출해 재활용. 최근-only 후보(score None)는 유사도 판정을 우회하고 recency로만 본다.
- 임계값 튜닝(골든셋 실측): `subcluster_threshold`는 이벤트 임베딩 코사인 분포를 측정해 결정.
  안전 밴드 **[0.30, 0.32]** — `<0.30`이면 이질 주제 병합(under-split, 원래 문제로 회귀),
  `>0.32`면 단일 주제 과분할(mixed_topics의 coding이 0.33부터 쪼개짐). 단일주제 파일 내부 최소
  코사인 floor ≈ 0.359. 과분할 경고(2026-07-05)를 존중해 밴드 중앙 **0.31** 채택.
  append 게이트 값은 라이브 Qdrant 점수 미측정이라 보수적 잠정값(검색 positive-floor 0.289 소폭 위) —
  실데이터 재세션화로 튜닝 필요, env 오버라이드 가능.
- 검증(골든 11개, 실 LLM+임베딩): **Assignment/Purity/Coverage 100%**(서브클러스터링이 지배하는
  클러스터링 품질 지표). mixed_topics가 travel/coding 2 클러스터로 정확히 분리돼 교차주제 메가 뭉침
  소멸. 노이즈 94.1%(shopping 1건 hold — 기존 A.X/EXAONE 비결정성). New-vs-Existing 81.8%(2건)는
  **서브클러스터링 회귀가 아님**을 실증: 두 케이스 모두 단일 클러스터라 LLM 입력이 기존과 동일하고,
  재실행 시 append↔create가 뒤집힘(비결정성) + EXAONE의 create-bias(프롬프트 "확실치 않으면 create").
- 검증 범위 분리(정직성): eval는 Qdrant 미사용이라 게이트를 적용하지 않음 → 게이트는 단위 테스트로
  검증(`_gate_appends`/`_append_blocked`), 서브클러스터러도 합성 벡터 단위 테스트. eval는 서브클러스터링
  + 프롬프트 품질을 본다. 전 경로(게이트 포함) 통합은 실데이터 재세션화가 담당(후속).
- 실데이터 검증(2026-08-06, 라이브 150개 재세션화 2회, DB 백업 후): **메가 뭉침 재발 없음** —
  before의 "여행 계획"(18, 항공권+브랜드로고+여름음악+맥미니+행성궤도 흡수)·"이터널 리턴"(33)이 두 run
  모두에서 분리되고, 분리된 주제(여름음악·맥미니·행성궤도·Tailscale)가 안정 재현. 골든 과적합 우려 해소.
  게이트는 단일일(08-05) 데이터라 사실상 미발동(배치 내 새 세션은 recent-path score None → 유사도 게이트
  우회, age 0일) → 이번 개선은 거의 전부 서브클러스터링 효과. 상세는 WorkLog 2026-08-06 "실데이터 검증".
- 남은 한계·관측: ① 게이트는 append→create 강등만 하고 create→append 승격은 안 함 → EXAONE
  create-bias로 인한 중복 세션(같은 주제 재생성)은 merge(후순위) 영역. ② 클러스터 *내부* LLM
  과분할(이터널리턴 내부 세분화가 run별 변동)은 서브클러스터링 밖의 LLM 변동성 — 잔존하나 교차주제 뭉침보다
  경미. ③ append 게이트 임계값 실효 검증·튜닝은 cross-day/오래된 후보 데이터 필요(단일일로는 미발동).
  ④ 부수: `extract_json` greedy 매칭이 JSON 뒤 추가 텍스트에 "Extra data" 실패→hold(재시도로 자연 해소).

## 2026-08-06 — [재검토] 2026-07-05 "임베딩 클러스터링 대신 LLM" 결정의 범위 한정

- 상태: 재검토(대체 아님) — 원 결정 유지, 적용 범위만 명확화
- 배경: 2026-07-05에 "짧은 제목·소표본에서 임베딩 밀도 클러스터링은 불안정"하다며 그룹핑을 LLM에 맡겼다.
  이번 서브클러스터링 도입이 그 결정과 충돌하는지 검토했다.
- 판단: 충돌하지 않는다. 2026-07-05 결정은 임베딩이 **최종 주제 그룹을 단독 생성**하는 것을 거부한 것이고,
  이번 서브클러스터링은 임베딩이 **명백히 다른 주제만 거칠게 선분리**하고 세밀한 판단은 여전히 LLM이
  수행하는 하이브리드다. 다만 그때의 "소표본 불안정" 경고는 유효하므로 임계값을 보수적으로 잡았다(0.31).
- 영향: 수동 스냅샷 경로의 `clusterer.cluster_tabs`(LLM 그룹핑)는 무변경. 서브클러스터링은
  상시수집→자동세션화 경로 전용.

## 2026-08-05 — [재확인] 그룹 간 과잉 append로 인한 세션 품질 불안정 (구조적, 미해결)

- 상태: 해결됨 (2026-08-06 "서브클러스터링 + append 게이팅 구현" 참조) — 프롬프트 튜닝 한계 도달로
  구조적 해법(1+2)을 구현·검증
- (원 관찰 기록 보존)
- 관찰: 동일 실데이터(139개)를 재세션화할 때 run마다 품질이 출렁인다. 어떤 run은 주제별로 잘 분리되고, 어떤 run은 "여행 계획"이 항공권+브랜딩+맥미니+음악+github을 흡수하는 메가 뭉침을 만든다. 원인은 나중 시간그룹이 먼저 만든 세션을 후보로 잡아 과도하게 append하는 것 + LLM 비결정성.
- 프롬프트 v4(과분할 금지)·v5(메일 분리)는 특정 케이스는 잡지만 그룹 간 append 불안정은 구조적으로 못 잡는다.
- 다음 후보(택1 결정 필요): ① 그룹 내 임베딩 서브클러스터링(다주제 선분리 후 클러스터별 LLM) ② append 게이팅(벡터 유사도 하한 + 시간 근접 요구 — 오래되거나 유사도 낮은 후보엔 append 금지) ③ 그룹 시간 gap 축소.

## 2026-08-06 — 메일 노이즈 규칙 title 신호 보강 (gmail·naver 받은편지함)

- 상태: 승인·구현·검증 (사용자 요청 2026-08-06)
- 배경: 실데이터 검수에서 `mail.google.com/mail/u/1/#inbox`(정규화 후 `/mail/u/1/`)와 `mail.naver.com/`
  루트가 경로에 목록 키워드가 없어 noise_filter를 통과, 항공권/가비아 세션에 받은편지함 스침이 새어들었다.
  2026-08-05 C안은 "gmail은 경로 구분 불가라 보존"으로 남겨뒀던 부분.
- 결정: `noise_filter._is_mail_list_view`에 **title 신호**(`_MAIL_LIST_TITLE_RE = ^(받은편지함|받은메일함)`)
  추가. 웹메일 호스트 && 개별 메일 읽기 경로 아님 && (목록 경로 || 받은편지함 title)이면 discard.
  개별 메일 읽기는 title이 메일 '제목'이라 이 패턴으로 시작하지 않아 보존된다. title 없는 애매한 gmail
  루트는 여전히 보존(기존 `test_gmail_root_ambiguous_survives` 유지).
- 대안: 경로/호스트 special-case(gmail 판별 불가로 부적합), 영어 "inbox"까지 포함(메일 제목 오탐 위험으로
  받은함 한국어 2종으로 한정).
- 검증: 단위 테스트 3종 추가(gmail/naver 받은편지함 noise, 개별 읽기 보존), 골든 `mail_inbox_refresh_is_noise`에
  gmail/naver 루트 케이스 추가(노이즈 100%), backend pytest 259 통과.

## 2026-08-06 — 세션 병합(Merge) 설계 (제안, 자동 금지·제안+확인·가역)

- 상태: 제안 (설계만, 구현은 별도 승인) — `docs/merge-design.md`
- 배경: 서브클러스터링으로 교차주제 메가 뭉침은 해소됐으나 반대 방향(같은 주제 과분할·중복 세션)이
  실데이터에 잔존(제주항공권 9+3, 나어나이 11+2, 낭만인프라 4+1). 원인=EXAONE create-bias + 게이트의
  append→create 단방향성 + 시간 그룹 경계. append 경로에서 못 고치므로 사후 병합이 올바른 계층.
- 결정(설계 원칙): **자동 병합 금지**(기존 원칙 + AGENTS §11), 시스템은 다중 신호(벡터 유사도 하한 +
  키워드/제목 겹침 + 시간 근접 가점)로 후보를 **제안만** 하고 사용자 확인 후 실행. **soft-delete
  (status='merged', merged_into) + undo**로 가역. 스키마는 additive ALTER. API 3단계(P1 제안 읽기전용 →
  P2 병합 실행 → P3 undo). 상세는 merge-design.md.
- 남은 사용자 결정: 후보 임계값(floor·겹침 필수 여부), 생존 세션 선택 정책, 제안 노출 시점(자동/수동),
  UI 위치. merge-design.md §9 표 참조.

## 2026-08-07 — 새 탭 오버라이드로 Orbit 홈 제공

- 상태: 승인
- 배경: `orbit_front` 시안의 메인 화면을 사용자가 실제로 만나는 진입점으로 삼으려면 익스텐션이
  브라우저의 시작 화면을 대신해야 한다. 기존 익스텐션은 사이드패널 진입점만 있었다.
- 결정: `chrome_url_overrides.newtab`으로 새 탭을 Orbit 홈으로 대체한다. 홈의 검색창은
  주소 입력 시 이동, 그 외에는 **사용자가 크롬에 설정한 기본 검색엔진**으로 검색한다
  (`chrome.search` API, `search` 권한 추가).
- 대안: (1) 별도 페이지로 두고 사용자가 직접 방문 — 진입점으로서 의미가 없음.
  (2) 구글 URL 고정 — 기본 엔진을 다른 것으로 쓰는 사용자에게 어긋남.
- 근거와 영향: 새 탭 오버라이드는 브라우저 전역 경험을 바꾸므로 사용자 승인을 받고 진행했다.
  되돌리려면 newtab 엔트리포인트만 삭제하면 된다. 스토어 심사 시 `search` 권한 사유 설명이 필요하다.
  주소/검색 판별은 `lib/omnibox.ts` 한 곳에 두어 이후 다른 진입점도 같은 규칙을 쓰게 했다.
  `javascript:`·`data:`·`chrome:` 등은 이동시키지 않고 검색으로 강등한다(경계 검증).

## 2026-08-07 — 홈 카드는 목업 데이터로 시작 (한시적)

- 상태: 승인 (한시적)
- 배경: 홈의 "최근 탐색"·"이어서 탐색" 카드에 실데이터를 붙이려면 로딩·빈·오류 상태와 인증 흐름까지
  범위가 커진다. 이번 작업의 목표는 디자인 이식과 검색 연결이었다.
- 결정: 시안의 더미 데이터를 `entrypoints/newtab/data/home-mock.ts`로 옮겨 쓴다. 모듈명과 주석에
  목업임을 명시하고, 백엔드 도메인 타입(`lib/types.ts`)과 섞지 않는다.
- 대안: 섹션 자체를 비우기 — 시안의 화면 구성이 사라진다.
- 근거와 영향: 백엔드가 꺼져 있어도 홈이 깨지지 않는다. 다만 **실데이터로 오인될 수 있는 상태**이므로
  실데이터 연결 시 이 모듈은 통째로 제거한다. 그때까지 열린 항목으로 남긴다.

## 2026-08-07 — 디자인 토큰을 시안 팔레트로 단일화

- 상태: 승인
- 배경: 익스텐션 토큰(`#f2660a`·`#f7f8fa`·회색 그림자)과 시안(샌드 `#fefaf6`·테라코타 `#f07550`·
  따뜻한 그림자)이 어긋나 같은 제품으로 보이지 않았다.
- 결정: 시안 팔레트를 단일 기준으로 삼는다. 사이드패널은 `tailwind.css`의 `@theme`, 새 탭 홈은
  `home.css`의 `:root`에 **같은 값**을 둔다. 시안에 없던 실패 상태 색은 팔레트 온도에 맞춰
  `--color-orbit-danger: #b8452b`로 정의한다.
- 대안: 두 화면이 하나의 CSS를 공유 — 엔트리포인트가 다르고 한쪽은 Tailwind, 한쪽은 순수 CSS라
  결합도만 올라간다.
- 근거와 영향: 값이 두 곳에 중복되므로 한쪽을 바꾸면 다른 쪽도 바꿔야 한다. 두 파일 주석에 명시했다.
  큰 반경(28px)은 좁은 사이드패널에서 과해 카드용 20px(`--radius-orbit-card`)로 조정했다.

## 2026-08-07 — 아틀라스는 시안을 그대로 이식 (해시 라우팅으로만 변형)

- 상태: 승인
- 배경: 1차에서 홈의 검색창만 연결하고 나머지를 비활성으로 두었으나, 사용자가 두 번째
  화면(아틀라스)과 네비게이션·마우스 상호작용까지 "목업 채로" 옮기기를 요청했다.
- 결정: 시안에서 **도달 가능한 컴포넌트 전부**를 원본 그대로 옮긴다. 변형은 확장에서
  동작할 수 없는 지점으로 한정한다 — ① 라우팅을 해시(`#/orbit-atlas`)로, ② Phosphor 를
  woff2 로컬 번들로, ③ 폰트를 시스템 스택으로, ④ 검색창을 실제 주소창처럼 동작하게.
  어디서도 import 되지 않는 죽은 코드 4개(`OrbitDetailPanel`·`OrbitAtlasCanvas`·
  `OrbitSidebar`·`DesignedByVariantChip`)는 옮기지 않는다.
- 대안: 필요한 부분만 발췌해 재작성 — 시안과의 시각적 차이가 누적되고 이후 시안 갱신을
  반영하기 어려워진다.
- 근거와 영향: 시안이 이미 완성도 있게 구현돼 있어 재작성 이득이 없다. 대신 두 저장소에
  같은 코드가 존재하게 되므로, 시안이 바뀌면 수동으로 다시 옮겨야 한다. 변형 지점은
  `Plan.md` "시안 대비 의도적 차이" 표에 정리했다.

## 2026-08-07 — Phosphor 아이콘은 woff2 로컬 번들

- 상태: 승인
- 배경: 시안은 Phosphor 를 CDN `<script>` 로 불러온다. MV3 CSP 에서 불가능하다.
  아틀라스와 더미 데이터에서 41종이 쓰여 lucide 매핑은 손실이 크다.
- 결정: `@phosphor-icons/web@2.1.2` 의 regular woff2 를 `public/fonts/` 에 넣고,
  패키지 style.css 에서 @font-face 만 다시 선언한 CSS 를 `styles/phosphor.css` 로 둔다.
  패키지 자체는 의존성으로 남기지 않는다(생성 시점에만 필요).
- 대안: (1) 패키지를 그대로 import — style.css 가 svg/ttf/woff/woff2 4종을 참조해
  번들이 4.1MB 늘어난다(svg 만 3MB). (2) lucide 41종 매핑 — 시안과 아이콘이 달라진다.
- 근거와 영향: 번들 4.76MB → 786KB. 갱신 절차는 `styles/phosphor.css` 상단 주석에 적었다.
  생성물을 저장소에 넣는 방식이라 업스트림 갱신이 자동으로 따라오지 않는다.

## 2026-08-07 — 새 탭 바로가기: topSites 초기값 + 사용자 편집 우선

- 상태: 승인
- 배경: 검색창 아래 `검색 범위`(목업의 미구현 컨트롤)를 크롬 새 탭과 같은 **바로가기**로
  교체해 달라는 요청. 펼침/접힘이 가능해야 한다.
- 결정: 사용자가 아직 편집한 적이 없으면 `chrome.topSites`(자주 방문한 사이트)를 보여주고,
  한 번이라도 추가·삭제하면 그때부터 `chrome.storage.local` 의 사용자 목록만 쓴다.
  크롬 새 탭과 같은 규칙이다. 상한 10개. 펼침 상태도 저장한다.
  파비콘은 `favicon` 권한 + 확장 내장 `_favicon/` 로 그린다.
- 대안: (1) 사용자 추가만 지원 — 첫 화면이 비어 첫인상이 나쁘다.
  (2) 외부 파비콘 서비스(google s2) — 방문 사이트 목록이 외부로 나간다.
- 근거와 영향: `topSites`·`favicon` 권한 2개가 추가된다. 둘 다 읽기 전용·로컬이며
  네트워크 요청을 만들지 않는다. topSites 를 빼면 초기 목록만 비게 되므로 되돌리기 쉽다.
  바로가기 주소는 `parseOmniboxInput` 으로 검증해 `javascript:`·`chrome:` 등 특권 스킴이
  바로가기로 저장되지 않게 막았다.

## 2026-08-07 — 세션 진입점을 대시보드 하나로 통일

- 상태: 승인
- 배경: 홈에서 세션을 여는 경로가 둘이었다 — 별도 상세 모달(`SessionDetail`)과
  대시보드(아틀라스). 같은 세션을 두 가지 화면으로 보여 주는 셈이라 정보 구조가 갈렸다.
- 결정: 상세 모달을 없애고 **모든 세션 진입을 대시보드로 통일**한다. 최근 탐색 항목,
  카드의 `상세 보기`, AI 응답 모두 `?orbit=&session=` 을 실어 대시보드로 이동한다.
  카드의 두 번째 버튼은 `이어서 탐색` 대신 **`세션 복원`** 으로 바꾸고, 상세 모달에 있던
  복원 조작(hover 시 `새 창으로` 펼침)을 그대로 옮긴다.
- 대안: 모달 유지 + 대시보드 이동을 별도 버튼으로 — 버튼이 셋이 되고 두 상세 화면이 계속 공존한다.
- 근거와 영향: 대시보드가 상세 모달의 정보를 모두 포함한다(요약·핵심 정보·할 일·페이지 목록).
  모달 전용 CSS 281줄과 컴포넌트 1개가 제거됐다. 복원은 사이드패널과 같은
  `lib/chrome-bridge.ts` 구현을 재사용해 두 화면의 동작을 일치시켰다.

## 2026-08-07 — 네비게이터 기본 닫힘, Orbit 그래픽 진입만 예외

- 상태: 승인
- 배경: 새 탭을 열 때마다 네비게이터 드로어가 펼쳐진 채로 떠 브라우저 시작 화면으로서
  방해가 됐다.
- 결정: 기본값을 닫힘으로 한다. 단 **히어로의 Orbit 그래픽으로 대시보드에 들어갈 때만**
  펼친 채 연다. 카드·타임라인에서 특정 세션으로 들어갈 때는 닫힌 채 둔다.
- 대안: 항상 닫힘 — 대시보드 전체를 둘러보러 갈 때 매번 토글해야 한다.
- 근거와 영향: 두 진입의 의도가 다르다. 그래픽은 "대시보드 전체 탐색", 카드·타임라인은
  "이 세션 보기"다. 아틀라스는 `nav.open` 을 강제하지 않고 공유 상태를 읽으므로
  진입점에서 `patchNavState({ open: true })` 하는 것만으로 구현된다.
  열림 상태는 모듈 스코프라 새 탭마다 초기화된다.

## 2026-08-07 — 인증: 구글 로그인 전용 + 자체 JWT, 로그인 필수

- 상태: 승인
- 배경: 백엔드 API 6개가 전부 무인증으로 열려 있었고, `user_id` 컬럼은 있으나 전 구간
  `"local"` 로 하드코딩돼 있었다. 스키마는 멀티유저를 염두에 뒀지만 주체가 없었다.
- 결정:
  - 로그인 수단은 **구글 계정만**. 비밀번호를 저장하지 않는다.
  - 익스텐션은 `chrome.identity.getAuthToken` 으로 access token 을 받고,
    백엔드가 이를 1회 검증한 뒤 **자체 JWT(HS256)** 를 발급한다.
  - 사용자 매칭 기준은 이메일이 아니라 **구글 `sub`** — 이메일은 바뀔 수 있다.
  - **로그인 필수.** 비로그인 상태에서는 화면도 API 도 열지 않는다.
  - 인증 도입 전 쌓인 `user_id="local"` 데이터는 **최초 가입자 한 명에게만** 이관한다.
- 대안: (1) `launchWebAuthFlow` — 팝업과 토큰 교환을 직접 구현해야 함.
  (2) 구글 ID 토큰을 매 요청 검증 — 만료 주기를 우리가 못 정하고 검증 비용이 매 요청 발생.
  (3) 로그인 선택(로컬 모드 병행) — 두 모드를 모두 유지해야 하고 API 경계가 계속 열려 있음.
- 근거와 영향:
  - `getAuthToken` 은 access token 을 주므로 백엔드가 `tokeninfo` 로 **`aud` 가 우리
    client_id 와 일치하는지 반드시 확인**해야 한다. 이 검사가 빠지면 다른 앱용으로 발급된
    토큰으로도 우리 계정에 로그인할 수 있다(confused deputy). 이번 구현의 핵심 방어선이며
    전용 테스트(`test_verify_rejects_token_issued_to_another_client`)로 고정했다.
  - 인증 의존성은 **라우터 단위**로 걸었다(`main.py`). 엔드포인트마다 붙이면 언젠가
    하나를 빠뜨리고 그 경로로 데이터가 샌다.
  - 신규 의존성 `PyJWT`. JWT 를 직접 구현하면 alg 혼동·타이밍 공격 같은 함정이 있다.
  - 배포 시에는 HTTPS 와 프로덕션 도메인 `host_permissions` 가 추가로 필요하다.

## 2026-08-07 — 데이터 격리: 모든 조회를 소유자로 제한

- 상태: 승인
- 배경: 인증만 붙이면 "로그인은 되는데 남의 데이터가 보이는" 상태가 된다.
  실제로 `sessions.py` 는 `user_id` 필터가 **아예 없었다**.
- 결정: 조회·수정·삭제 전 구간에 소유자 조건을 넣는다.
  - 세션 단건 접근은 `_owned_session()` 을 거치고, 남의 세션은 **403이 아니라 404** 로 답한다
    (403은 "그 id의 세션이 존재한다"는 사실을 알려준다).
  - 벡터 검색 결과는 Qdrant 에 소유자 정보가 없으므로 **SQL 단계에서 반드시 거른다**.
  - 배치 파이프라인은 **한 배치에 한 사용자** 이벤트만 claim 한다. 시간 gap 그룹화가
    사용자 경계를 모르기 때문에 섞으면 서로 다른 사람의 방문이 한 세션으로 묶인다.
  - 후보 세션 검색·병합 제안·자동 병합도 같은 사용자 안에서만 수행한다.
- 근거와 영향: 26개 보호 경로 전부에 대해 토큰 없음/위조 토큰 두 경우를 실제 요청으로
  검증하는 테스트(`test_auth_boundary.py`, 56건)를 뒀다. 라우터를 새로 추가해도
  이 테스트가 자동으로 커버한다.

## 2026-08-07 — 추천 세션: 다중 신호 후보 + LLM 리랭킹, 협업 필터링 미도입

- 상태: 승인
- 배경: 추천을 최신순이나 단일 모델로 만들면 "방금 본 것"만 반복 노출된다. Orbit은
  다수 사용자 간 콘텐츠 추천이 아니라 **단일 사용자의 탐색 연속성**이 핵심이다.
- 결정: `후보 스코어링(규칙+임베딩) → 상위 10~20개 → LLM 리랭킹 → 3개` 구조.
  1차 점수 = `0.35*similarity + 0.30*unfinished + 0.15*recency + 0.10*revisit + 0.10*current_context`.
  최종 3개는 성격(continue / related / rediscover)을 섞고 각각 **추천 이유**를 붙인다.
  Bayesian·collaborative filtering은 초기 버전에서 쓰지 않는다.
- 대안: 최신순 정렬(맥락 무시), 임베딩 유사도 단독(미완료·반복 신호 소실),
  전 후보를 LLM에 투입(비용이 세션 수에 비례).
- 근거와 영향: 1차 점수는 DB·임베딩만 쓰므로 저렴하고, LLM은 추천 경로에서 한 번만 부른다.
  가중치와 신호 정의는 테스트로 고정했다(`test_recommender_scoring.py` 22건) —
  가중치를 바꾸면 테스트가 깨지도록 의도했다.
  LLM 응답은 신뢰하지 않는다: 인덱스 범위·중복·성격·이유 길이를 검증하고 어긋나면
  규칙 기반으로 폴백한다. 추천이 안 나오느니 덜 정교한 추천이 낫다.
  향후 implicit feedback(클릭·복원·무시·체류시간)이 쌓이면 개인별 weight 학습이나
  bandit으로 확장할 수 있도록 신호를 분리해 두었다.

## 2026-08-07 — 추천 갱신: 서버 캐시 + 지연 재계산 (stale-while-revalidate)

- 상태: 승인 (갱신 시점은 사용자가 "추천대로 구현"으로 위임)
- 배경: 새 탭은 하루에 수십 번 열린다. 열 때마다 LLM을 부르면 비용이 열람 횟수에
  비례하고 화면도 매번 로딩을 기다린다. 반대로 완전 고정이면 방금 탐색한 맥락이 빠진다.
- 결정: 추천 결과를 사용자별로 DB에 캐시(TTL 30분, `RECOMMENDATION_TTL_MINUTES`).
  새 탭을 열면 **캐시를 즉시 반환**하고, TTL이 지났으면 응답은 그대로 준 뒤
  **백그라운드에서 재계산**한다. 다음 새 탭부터 새 추천이 보인다.
  화면의 3개 캐러셀 회전은 이미 받은 결과를 돌리는 것이라 추가 호출이 없다.
  동기화 배치가 세션을 갱신하면 캐시를 낡은 것으로 표시한다(삭제가 아니라 `computed_at`
  을 과거로 미는 방식 — 다음 요청이 빈 화면을 거치지 않는다).
- 대안: 매 요청 계산(비용·지연), 고정 TTL 후 동기 재계산(그 요청만 느려짐),
  클라이언트 폴링(호출 증가).
- 근거와 영향: LLM 호출이 **사용자당 시간당 최대 2회**로 묶인다. 최초 요청만 동기
  계산하는데, 이는 빈 화면을 보여주지 않기 위한 의도적 예외다.
  캐시 정책은 테스트로 고정했다(`test_recommender_cache.py`) — 신선한 캐시인데
  재계산하면 실패한다.

## 2026-08-07 — 확장 ID 를 manifest key 로 고정하고, key 는 소스에 커밋

- 날짜: 2026-08-07
- 상태: 승인
- 배경: `manifest.key` 가 없어 확장 ID 가 설치 경로에서 유도됐다. `pnpm dev`
  (`.output/chrome-mv3-dev`)와 `pnpm build`(`.output/chrome-mv3`)의 ID 가 서로 다른데
  구글 OAuth 클라이언트에는 항목 ID 를 하나만 등록하므로 둘 중 한쪽에서만 로그인이 됐다.
  재설치·경로 이동·다른 팀원 머신에서도 동일하게 깨진다. 팀원이 각자 환경에서 각자 구글
  계정으로 테스트해야 하는 요구가 확인되면서 선결 조건이 됐다.
- 결정:
  - RSA 2048 공개키(SPKI DER base64)를 `manifest.key` 로 고정한다.
    확장 ID = `aghgamoeifieijjckhpfkmhiibacnhml`.
  - 그 값을 **`.env` 가 아니라 `wxt.config.ts` 에 커밋**한다.
  - 개인키는 `extension/.keys/`(gitignore)에 보관한다.
- 대안:
  (1) `client_id` 처럼 `.env` 에서 읽기 — 팀원이 각자 채워야 하고, 빠뜨리거나 다른 값을 넣으면
      ID 가 갈려 애초에 막으려던 문제가 그대로 재발한다. `client_id` 는 환경마다 달라질 수
      있는 값이지만 `key` 는 **모든 환경에서 같아야 하는 값**이라 성격이 반대다.
  (2) 웹스토어에 먼저 비공개 업로드해 스토어 발급 키를 사용 — 개발·배포 ID 가 영구 일치하지만
      개발자 계정 등록·수수료·심사 대기가 선행된다. 지금 필요한 것은 로컬 팀 테스트다.
  (3) `launchWebAuthFlow` 전환으로 우회 — redirect URI 가
      `https://<extension-id>.chromiumapp.org/` 라 **확장 ID 의존이 그대로**여서 대안이 못 된다.
  (4) 개발용 인증 우회 플래그 — 인증 방어선을 무력화하는 경로를 상시 유지하게 된다. 기각.
- 근거와 영향: 공개키는 비밀이 아니므로 커밋해도 노출 위험이 없고, 오히려 커밋해야 팀 전체가
  같은 ID 를 갖는다. dev·build 산출물에서 키로부터 ID 를 직접 유도해 일치를 검증했다.
  웹스토어 배포 시에는 스토어가 발급한 키로 교체하고 OAuth 항목 ID 를 한 번 갱신해야 한다.
  `getAuthToken` 은 크롬 프로필 계정에 묶이지만, 팀원이 각자 머신·각자 프로필에서 테스트하는
  구도에서는 제약이 되지 않는다. 한 사람이 여러 계정을 오갈 때는 크롬 프로필을 분리한다.

## 열린 결정

| 항목 | 필요한 결정 | 구현 전 조건 | 상태 |
|---|---|---|---|
| LLM 주제 뭉침(그룹 간 과잉 append) | ~~서브클러스터링 / append 게이팅 / gap 축소 중 택1~~ | 1+2(서브클러스터링+append 게이팅) 구현·골든 검증 | 결정됨 (2026-08-06, 위 "서브클러스터링 + append 게이팅 구현"). 잔여: append 게이트 임계값 실데이터 튜닝, EXAONE create-bias 중복세션은 merge 영역 |
| 세션 분류 단위 | 큰 주제 또는 완결된 세부 작업 | 대표 예시와 골든셋 합의 | 결정됨 (2026-08-03, 위 "제품 방향 전환" 항목 참조 — 배치 LLM 의도 분석이 그룹별로 판단) |
| 기존 세션 병합 | 후보 임계값·생존 세션 선택·제안 노출 시점·UI 위치 | `docs/merge-design.md` §9 결정 후 P1 착수 | 설계됨 (2026-08-06, "세션 병합 설계" — 자동 금지·제안+확인·가역 확정, 세부 임계값/UX는 결정 대기) |
| 페이지 본문 보관 | 보관 기간과 재요약 가능 기간 | 개인정보 정책 합의 | 미해결 |
| 외부 배포 보안 | 인증 방식과 허용 origin | 배포 환경 확정 | 미해결 |
| 팀 다중 환경 백엔드 접속 | 팀원별 로컬 백엔드 / 공유 백엔드 중 택1 (공유 시 `host_permissions`·`VITE_API_BASE_URL`·`JWT_SECRET` 공유 정책 동반) | 확장 ID 고정 완료 | 미해결 (2026-08-07) |
| `JWT_SECRET` 교체 | ~~현재 값이 `.env.example` 안내 명령 문자열 리터럴이라 예측 가능~~ | 사용자 승인 | 해결됨 (2026-08-07, 난수 64자로 교체 후 재기동. 구 키로 서명한 위조 토큰이 차단되는 것을 확인) |
| 웹 복원 | 안내 CTA 또는 제한적 복원 | 제품 범위 확정 | 미해결 |
| 홈 실데이터 연결 | ~~Session/Page 매핑, 로딩·빈·오류 상태~~ | 기존 세션·이벤트 API | 결정됨 (2026-08-07, Session → Page 2단계) |
| 홈 비활성 컨트롤 | ~~알림·설정·프로필·검색범위·AI 모드·아틀라스 진입의 동작 정의~~ | 시안 전체 이식 | 대부분 해소됨 (AI 모드는 Memory 검색 연결. 잔여: 알림·설정 버튼) |
| 사이드패널 파비콘 | 외부 서비스(google s2) → 확장 내장 `_favicon/` 전환 여부 | `favicon` 권한은 이미 있음 | 미해결 (새 탭은 내장 파비콘 사용 중, 사이드패널만 외부 요청) |
| 노이즈 사전 필터 | ~~체류 임계 미만 + SNS/광고성 도메인 이벤트를 LLM 무호출로 discard~~ | 구제 조건 + 규칙 3종으로 구현 | 결정됨 (2026-08-05, 위 "노이즈 사전 필터 도입" 항목 — 골든셋 노이즈 제외율 100% 고정) |
| EXAONE 엔드포인트 지연 | ~~dedicated ~60초/호출 문제~~ | serverless 전환으로 해소 | 결정됨 (2026-08-05, 아래 "EXAONE serverless 전환" 항목) |
