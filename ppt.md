# Orbit PPTX 요약

> 2026년도 인공지능 루키(AI Rookie) 대회 · 국내 AI 트랙
> 기준 시점: 2026-08-13 (본선 중간 결과물). 이 문서는 구현된 내용만 적는다.
> 계획 단계 서술은 §6 "남은 계획"에만 둔다.

## 1. 서비스 개요

Orbit은 사용자의 웹 탐색 이벤트(방문·체류)를 상시 기억하고, AI가 의미 있는 탐색 세션으로 자동 재구성해 필요할 때 자연어로 다시 찾아 복원할 수 있게 하는 Chrome Extension 기반 Personal Exploration Memory 서비스다. "지금 열린 탭을 즉시 세션으로 저장"하는 기존 스냅샷 기능도 그대로 제공한다.

한 줄 설명:
탐색의 흐름을 AI가 상시 기억하는 Personal Exploration Memory

핵심 콘셉트:
브라우저 탭과 방문은 단순한 링크 목록이 아니라 사용자의 사고 흐름이며, Orbit은 이 흐름을 AI가 상시 기억하고 필요한 순간 되짚어 복원하는 Personal Exploration Memory(AI Exploration OS)이다.

---

## 2. 문제 정의

현대 사용자는 자료 조사, 논문 탐색, 쇼핑 비교, 여행 계획, 업무 리서치 등을 하면서 수십 개의 탭을 동시에 열어둔다. 그러나 시간이 지나면 각 탭을 왜 열었는지, 어떤 작업 흐름에 포함되어 있었는지 기억하기 어렵다.

주요 문제는 다음과 같다.

1. 세션 맥락 소실
   - 작업을 중단했다가 다시 돌아왔을 때 어떤 탭을 왜 열었는지 기억하기 어렵다.

2. 저장하지 않은 탐색의 증발
   - 저장 버튼을 누른 것만 남는다. 닫아버린 탭, 검색하다 흘려보낸 경로는 복구할 방법이 없다.

3. 탐색 경로 검색 불가
   - 북마크와 방문 기록은 URL·제목으로만 찾을 수 있고, "무엇을 하려던 흐름이었는지"로는 찾을 수 없다.

문제의 핵심:
현재 브라우저는 탭과 URL은 기록하지만, 사용자의 탐색 목적과 사고 흐름은 이해하지 못한다.

---

## 3. Orbit의 해결 방식

Orbit은 방문 이벤트를 상시 수집해 AI가 의미적으로 관련 있는 이벤트끼리 자동으로 묶고, 이를 탐색 세션(Auto Session)으로 자동 생성·성장시킨다. 사용자가 지금 열린 탭을 즉시 묶어 저장하는 기존 방식도 함께 지원한다.

구현된 기능:

1. 상시 이벤트 수집
   - webNavigation 기반 방문 감지(opt-in, 기본 off), IndexedDB 로컬 큐, 수동·주기·개수·유휴 4트리거 배치 동기화.

2. Auto Session (자동 세션 생성·성장)
   - 배치마다 국내 LLM이 이벤트 그룹을 append(기존 세션에 추가)/create(신규 생성)/hold(보류)/discard(제외)로 판단해 사용자 개입 없이 세션을 생성·갱신한다.

3. Orbit 홈 (새 탭 대체)
   - 크롬 새 탭을 대체하는 시작 화면. 검색어는 사용자의 기본 검색엔진으로, 주소는 그대로 이동한다. 아래에 자주 방문한 사이트 기반 바로가기(추가·삭제·접기)를 붙인다.

4. Orbit 아틀라스
   - 세션을 중심 노드로, 방문 페이지를 시간순 궤도로 그리는 시각화 화면. 좌측 세션·페이지 네비게이터, 캔버스, 페이지 트레이, 상세 패널로 구성된다.

5. Exploration Timeline
   - 사이드패널 기본 화면. 날짜별 이벤트 스트림 + 세션 배지 + 수집/동기화 상태 카드. 세션 상세에서 탐색 경로를 시간순으로 복기한다.

6. 세션 이름 자동 생성 및 컨텍스트 요약
   - 국내 LLM이 세션의 탐색 목적, 핵심 정보, 미완료 작업, 다음 행동을 요약하고 세션 이름을 생성한다.

7. Search by Intent
   - 자연어로 검색하면 세션(벡터 검색)과 관련 방문 기록을 함께 반환한다.

8. Ask AI
   - 저장된 세션 요약과 실제 페이지 본문 조각을 근거로 답변을 스트리밍하고, 답변 아래 관련 세션을 최대 3개까지 표시한다. 통합 의도 라우터가 열린 탭 이동·세션 찾기·전체 기록 내용 검색·특정 세션 내용 검색을 구분한다. 각 질문은 이전 답변을 참조하지 않는 독립 단일턴이다.

9. 열린 탭 찾기·북마크
   - "아까 보던 영상으로 돌아가자"처럼 입력하면 열린 탭을 의미 검색해 이동한다. 비슷한 탭이 여러 개면 자동 이동하지 않고 최대 3개 후보를 제시해 사용자가 고르게 한다. 선택한 탭을 Chrome 북마크에 중복 없이 일괄 추가할 수도 있다.

10. 세션 병합
    - 같은 주제로 나뉜 세션을 확인해 개별 또는 일괄 병합하고, 성공 직후 되돌릴 수 있다. 명백한 중복만 처리하는 자동병합은 설정에서 opt-in한다.

11. Exploration Analytics
    - 주제별 탐색 시간과 자주 보는 도메인을 사이드패널 요약 카드에서 확인한다.

12. 즉시 스냅샷 저장 (기존 기능 유지)
    - 지금 열린 탭을 AI 임베딩·클러스터링으로 즉시 세션으로 묶어 저장하고 복원한다.

---

## 4. 주요 사용자 경험

주 경로는 사용자가 아무것도 하지 않는 것이다. 수집을 한 번 켜두면 브라우징 도중 방문 이벤트가 로컬 큐에 쌓이고, 동기화 시점에 배치로 분석되어 세션이 스스로 만들어지고 자라난다.

Orbit이 만들어내는 세션 예시:
- RTX 5070 비교
- 일본 여행 준비
- AI 논문 조사

각 세션에는 다음 정보가 포함된다.

- 세션 제목
- 탐색 목적
- 핵심 요약
- 주요 페이지 목록
- 미완료 작업
- 다음 행동

이후 사용자는 사이드패널에서 자연어로 "어제 보던 RTX 비교 자료 다시 열어줘"라고 입력해 관련 세션을 찾고 탭을 복원한다. 지금 열린 탭을 즉시 묶고 싶을 때는 스냅샷 저장 경로를 쓴다.

---

## 5. 기술 구조

파이프라인은 다음 순서로 동작한다.

```
이벤트 수집 → 인제스트(필터·정규화) → 시간 그룹화 → 임베딩 서브클러스터링
→ 후보 세션 검색 → LLM 의도 분석 → 세션 생성/갱신 → 요약 → passage 임베딩
→ Qdrant 색인 → 검색 → 선택적 리랭킹
```

1. 이벤트 수집
   - webNavigation 기반 방문 이벤트 상시 수집(Extension) → IndexedDB 로컬 큐 → 배치 동기화.
   - (즉시 저장 경로) DOM Parsing, Readability.js로 열린 탭 본문 추출.

2. 인제스트
   - 시스템 URL·비 http(s) 제외, 추적 파라미터 제거, 검색어 추출, 중복 병합.

3. 시간 그룹화
   - 30분 공백을 기준으로 이벤트를 시간 그룹으로 자른다.

4. 임베딩 서브클러스터링
   - 한 시간 그룹에 이질 주제(항공권 + 코딩 등)가 섞이면 LLM이 한 세션으로 뭉치는 불안정이 반복 재현됐다. 이를 막기 위해 이벤트 임베딩으로 average-linkage 응집 클러스터링을 돌려 명백히 다른 주제만 먼저 갈라낸다(평균 코사인 임계값 0.31).
   - HDBSCAN 같은 밀도 기반 클러스터링은 쓰지 않는다. 소표본·짧은 제목에서 불안정했기 때문이다. 임베딩은 거친 선분리만 맡고, 세밀한 판단은 LLM이 한다.

5. 후보 세션 검색
   - 클러스터 대표 벡터(성분별 평균)로 Qdrant에서 기존 세션 후보를 찾고, 최근 활성 세션을 함께 넣는다.

6. LLM 의도 분석
   - 클러스터별로 append / create / hold / discard 중 하나를 판단한다.
   - LG K-EXAONE(primary) → SKT A.X-K1(fallback).

7. 세션 생성/갱신
   - 판단 결과를 세션에 반영하고 이벤트-세션 매핑과 버전을 기록한다.

8. LLM 요약
   - 변경된 세션만 다시 요약한다. SKT A.X-K1(primary) → LG K-EXAONE(fallback).

9. passage 임베딩 · Qdrant 검색
   - 세션 요약을 Upstage `embedding-passage`로 색인하고, 검색은 `embedding-query`로 한다(비대칭 임베딩). Qdrant에서 코사인 유사도 기반 벡터 검색.

10. 선택적 리랭킹
    - 검색 상위 후보를 쿼리 관련성 순으로 재정렬한다(옵션). SKT A.X-K1(primary) → LG K-EXAONE(fallback).

핵심 전략:

- **방문마다 LLM을 부르지 않는다.** 세션화 분석은 동기화 트리거 시점의 배치에서만 수행한다. 비용과 호출 제한을 동시에 푸는 설계다.
- **두 국내 LLM을 서로의 폴백으로 묶는다.** 단계별로 우세한 모델을 primary로 두되 반대쪽을 fallback으로 걸어, 한 공급자가 중단돼도 기능이 멈추지 않는다.
- **레이트 리밋을 구조로 흡수한다.** A.X-K1의 3 RPS 제한에 맞춰 전역 리미터로 LLM 호출을 직렬화한다.
- **Structured Output 강제 대신 방어적 파싱.** 도구 호출을 강제하지 않고 프롬프트로 JSON 응답을 요청한 뒤, 응답에서 JSON을 추출·검증하고 실패 시 fallback한다.
- **AI 출력으로 파괴적 동작을 자동 실행하지 않는다.** 탭 이동은 확신이 낮으면 자동 실행 대신 후보를 제시하고, 세션 자동병합은 기본 off에 되돌리기를 제공한다.

---

## 6. 구현 현황과 남은 계획

2인 팀. 예선 이후 본선 중간 결과물까지 아래 범위를 구현했다.

### 구현 완료

- Chrome Extension MV3 — 사이드패널, 새 탭 홈, 아틀라스 시각화
- 상시 이벤트 수집 + IndexedDB 로컬 큐 + 4트리거 배치 동기화
- 배치 세션화 파이프라인(시간 그룹화 → 서브클러스터링 → 후보 검색 → 의도 분석 → 세션 갱신)
- 세션 요약·이름 자동 생성, passage 임베딩, Qdrant 벡터 검색
- Search by Intent, Ask AI(근거 기반 스트리밍 답변 + 의도 라우팅), Exploration Analytics
- 세션 병합(제안·개별/일괄·되돌리기·opt-in 자동병합)
- 열린 탭 의미 검색·이동, 북마크 일괄 추가
- 스냅샷 저장/복원, 구글 로그인, 민감 도메인 본문 제외
- 백엔드 자동화 테스트 488개(외부 API·DB·LLM 호출은 전부 대역 처리)
- 골든셋 기반 평가 하네스 4종 — 세션 분류(케이스 11개), 검색 리콜, Ask 의도 라우팅,
  열린 탭 이동. 병합 임계값 튜닝 스크립트 포함. 임계값은 추측이 아니라 이 하네스의
  실측 분리 구간에서 정했다(예: 검색 0.28, 서브클러스터링 0.31, 병합 제안 0.52).

### 남은 계획

- 소스 어댑터 — 북마크·PDF·GitHub를 같은 이벤트 인제스트 경로로 흡수한다. 파이프라인과 검색은 소스를 가리지 않게 설계돼 있어 어댑터만 추가하면 된다.
- 대화 이벤트 — ChatGPT·Gemini 대화를 이벤트로 받아 방문 기록과 한 타임라인에 병렬 표시한다.
- Team Workspace — 세션 단위 read-only 공유. `user_id`가 이미 전 테이블에 있어 인증·권한 계층만 추가하면 된다.
- 운영 전환 — 현재 스키마 반영은 `create_all` + 멱등 ALTER 러너다. 실사용 데이터가 쌓이면 Alembic 기반 마이그레이션으로 전환한다.

---

## 7. 국내 AI 모델 선택 근거

한국어 중심 브라우저 환경에서는 자연스러운 세션 분류와 맥락 이해를 위해 한국어 특화 AI 모델이 필요하다. 대회 연계기업 5곳 가운데 SKT · LG AI연구원 · 업스테이지 3곳의 모델을 채택했고, 파이프라인의 LLM·임베딩 호출은 전부 국내 모델이다.

### 예선 시점 후보 조사

1. SKT A.X K1 — 519B-A33B, Long Context, Tool Calling 특화. 적합성 매우 높음
2. Upstage Solar Pro 3 — 102.6B-A12B, 128K Long Context, Tool Calling 지원. 적합성 높음
3. LG K-EXAONE — 236B-A23B, Long Context. Tool Calling 명시는 제한적. 적합성 보통
4. KT Mi:dm K 2.0 — 11.5B, Long Context 제한적. 적합성 낮음

### 실측 후 확정된 구성

문서 스펙만으로 정한 예선 시점 판단은 실측에서 두 군데가 뒤집혔다.

- **EXAONE을 "보통"에서 의도 분석 primary로 올렸다.** 튜닝한 프롬프트에서 A.X-K1과 품질이 대등했고, 노이즈 이벤트를 discard하는 판단은 EXAONE이 더 나았다. 대신 요약·리랭킹은 A.X-K1이 우세해 단계별로 primary를 갈랐다.
- **Solar Pro 3는 LLM 경로에서 빼고 Upstage는 임베딩 전용으로 두었다.** LLM 2종을 A.X-K1과 EXAONE으로 확정하면서 두 모델을 서로의 폴백으로 묶는 편이 공급자 장애에 더 강했다.

| 파이프라인 단계 | Primary | Fallback |
|---|---|---|
| 세션 의도 분석 | LG K-EXAONE | SKT A.X-K1 |
| 탭 클러스터링(스냅샷) | LG K-EXAONE | SKT A.X-K1 |
| 세션 요약·제목 생성 | SKT A.X-K1 | LG K-EXAONE |
| Ask AI 답변 스트리밍 | SKT A.X-K1 | LG K-EXAONE |
| 검색·추천 리랭킹 | SKT A.X-K1 | LG K-EXAONE |
| 저장 임베딩 | Upstage `embedding-passage` | — |
| 검색 임베딩 | Upstage `embedding-query` | — |

운영상 확인한 제약 두 가지:

- A.X-K1은 3 RPS 제한이 있어 배치 파이프라인의 LLM 호출을 전역 리미터로 직렬화한다.
- K-EXAONE은 FriendliAI serverless로 호출한다. dedicated 엔드포인트는 웜 상태에서도 호출당 약 60초라 대화형 UX에 맞지 않았다. 또 hybrid reasoning 모델이라 분류·요약에서는 추론 트레이스를 꺼야 한다. 켜두면 출력 토큰을 트레이스가 소진해 본문이 비어 온다.

---

## 8. 차별화 포인트

Orbit은 기존 탭 관리 도구와 다르다.

기존 방식:
- 북마크에 직접 저장
- 탭 그룹을 수동 생성
- URL이나 제목으로만 검색
- 탭을 닫으면 맥락이 사라짐

Orbit 방식:
- 방문 이벤트를 상시 기억하고 AI가 세션으로 자동 생성·성장(Auto Session)
- 저장 판단을 하지 않고 닫아버린 탐색도 세션으로 재구성
- 자연어로 세션과 개별 방문 기록을 함께 검색(Search by Intent)
- 세션 요약과 페이지 본문을 근거로 답변(Ask AI)
- 세션 복원으로 탐색 맥락 유지, 탐색 패턴은 Analytics로 확인

핵심 메시지:
브라우저 탭과 방문은 단순한 링크가 아니라 사용자의 사고 흐름이다. Orbit은 이 사고 흐름을 AI가 상시 기억하고 복원하는 Personal Exploration Memory다.

---

## 9. 개발 원칙과 데이터 처리

브라우저 전체 방문 기록을 다루는 서비스이므로 수집 범위를 기본값에서부터 좁게 잡았다.

- **수집은 opt-in, 기본 off다.** 사용자가 켜기 전까지 방문 이벤트를 한 건도 수집하지 않는다. 자동 동기화도 기본 off다.
- **민감 도메인·경로는 본문을 수집하지 않는다.** 금융·인증 URL은 본문만 제거하고 방문 이벤트(제목·URL)는 남겨 세션 복원을 유지한다. 판정 규칙은 확장과 백엔드에 이중 구현해 확장을 우회한 요청도 서버에서 다시 걸러진다.
- **로컬 우선 큐.** 이벤트는 IndexedDB에 먼저 쌓이고 동기화 성공 후에만 정리된다. MV3 서비스 워커가 종료돼도 유실되지 않으며, 동기화 전까지 데이터는 기기를 벗어나지 않는다.
- **수집 대상 제한과 정규화.** http/https만 수집하고, 추적 파라미터(`utm_*`, `gclid`, `fbclid`)는 인제스트 시점에 제거한다.
- **AI 출력을 신뢰하지 않는다.** 구조화 출력은 파싱·검증하고 실패 시 fallback하며, AI 판단으로 파괴적 동작을 자동 실행하지 않는다.

---

## 10. Orbit의 최종 목표

Orbit은 브라우저를 단순 웹 탐색 도구에서 사용자의 정보 탐색 과정과 사고 흐름을 기억하는 AI Workspace로 확장하는 것을 목표로 한다.

최종적으로 사용자는 다음과 같은 경험을 해야 한다.

"내가 전에 어떤 목적으로 어떤 자료를 보고 있었는지 AI가 기억하고, 필요한 순간 자연어로 다시 찾아서 작업을 이어갈 수 있게 해준다."
