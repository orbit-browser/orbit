# Orbit PPTX 요약

## 1. 서비스 개요

Orbit은 사용자의 웹 탐색 이벤트(방문·체류)를 상시 기억하고, AI가 의미 있는 탐색 세션으로 자동 재구성해 필요할 때 자연어로 다시 찾아 복원할 수 있게 하는 Chrome Extension 기반 Personal Exploration Memory 서비스다. "지금 열린 탭을 즉시 세션으로 저장"하는 기존 스냅샷 기능도 그대로 제공한다.

한 줄 설명:
탐색의 흐름을 AI가 상시 기억하는 Personal Exploration Memory

핵심 콘셉트:
브라우저 탭과 방문은 단순한 링크 목록이 아니라 사용자의 사고 흐름이며, Orbit은 이 흐름을 AI가 상시 기억하고 필요한 순간 되짚어 복원하는 Personal Exploration Memory(AI Exploration OS)이다.

---

## 2. 문제 정의

현대 사용자는 자료 조사, 논문 탐색, 쇼핑 비교, 여행 계획, 업무 리서치 등을 하면서 수십 개의 탭을 동시에 열어둔다. 그러나 시간이 지나면 각 탭을 왜 열었는지, 어떤 작업 흐름에 포함되어 있었는지 기억하기 어렵다.

주요 문제는 다음과 같다.

1. 세션 맥락 소실
   - 작업을 중단했다가 다시 돌아왔을 때 어떤 탭을 왜 열었는지 기억하기 어렵다.

2. 중복 탭 과잉
   - 같은 주제의 탭이 반복 생성되며 브라우저 성능과 사용성을 저하시킨다.

3. 탭 검색 불가
   - 수십 개의 탭 중 원하는 탭을 찾으려면 직접 하나씩 확인해야 한다.

문제의 핵심:
현재 브라우저는 탭과 URL은 기록하지만, 사용자의 탐색 목적과 사고 흐름은 이해하지 못한다.

---

## 3. Orbit의 해결 방식

Orbit은 방문 이벤트를 상시 수집해 AI가 의미적으로 관련 있는 이벤트끼리 자동으로 묶고, 이를 탐색 세션(Auto Session)으로 자동 생성·성장시킨다. 사용자가 지금 열린 탭을 즉시 묶어 저장하는 기존 방식도 함께 지원한다.

주요 기능:

1. Auto Session (자동 세션 생성·성장)
   - 방문 이벤트를 배치로 모아 LLM이 의미 단위로 묶고, 세션을 사용자 개입 없이 자동 생성·갱신한다.

2. Exploration Timeline
   - 사이드패널 홈에서 날짜별 방문 이벤트와 세션 배지를 시간 역순으로 확인한다.

3. 세션 이름 자동 생성 및 컨텍스트 요약
   - 국내 LLM이 세션의 탐색 목적, 핵심 정보, 미완료 작업, 다음 행동을 요약하고 세션 이름을 생성한다.

4. Search by Intent
   - 사용자가 "지난주에 봤던 항공권 탭", "어제 보던 RTX 비교 자료"처럼 자연어로 검색하면 관련 세션과 개별 방문 기록을 함께 찾는다.

5. 원클릭 세션 복원
   - 저장된 세션의 대표 탭을 즉시 다시 열 수 있다.

6. Exploration Analytics
   - 주제별 탐색 시간, 자주 보는 사이트, 반복 방문/검색, 일별 탐색량 추이를 확인한다.

7. 즉시 스냅샷 저장 (기존 기능 유지)
   - 지금 열린 탭을 AI 임베딩·클러스터링으로 즉시 세션으로 묶어 저장한다.

---

## 4. 주요 사용자 경험

사용자는 여러 탭을 열어둔 상태에서 단축키 또는 Extension 버튼으로 Orbit을 실행한다.

Orbit은 현재 열린 탭을 분석한 뒤 다음과 같은 주제별 세션을 생성한다.

예시:
- RTX 5070 비교
- 일본 여행 준비
- AI 논문 조사

각 세션에는 다음 정보가 포함된다.

- 세션 제목
- 탐색 목적
- 핵심 요약
- 주요 탭 목록
- 미완료 작업
- 다음 행동

사용자는 이후 자연어로 “어제 보던 RTX 비교 자료 다시 열어줘”라고 입력하면 관련 세션을 검색하고 탭을 복원할 수 있다.

---

## 5. 기술 구조

Orbit은 다음 파이프라인으로 동작한다: 이벤트 수집 → 배치 의도 분석 → 세션 생성/갱신 → 요약 → passage 임베딩 → Qdrant 검색 → 선택적 리랭킹.

1. 이벤트 수집
   - webNavigation 기반 방문 이벤트 상시 수집(Extension) → 로컬 큐 → 배치 동기화
   - (즉시 저장 경로) DOM Parsing, Readability.js로 열린 탭 본문 추출

2. 배치 의도 분석
   - 이벤트 그룹별로 LLM(Solar Mini)이 append(기존 세션에 추가)/create(신규 생성)/
     hold(보류)/discard(제외) 중 하나를 판단한다.
   - HDBSCAN 등 별도 클러스터링 알고리즘이나 cosine 유사도 임계값 규칙을 쓰지 않고,
     LLM 프롬프트 기반 판단으로 그룹핑한다(즉시 저장 경로의 탭 클러스터링도 동일하게
     Solar Mini LLM이 주제별로 묶는다).

3. 세션 생성/갱신
   - 의도 분석 결과를 세션에 반영하고 이벤트-세션 매핑을 기록한다.

4. LLM 요약
   - SKT A.X-K1(primary) → Upstage Solar Pro 3(fallback)

5. passage 임베딩
   - Upstage embedding-passage로 세션을 저장하고, 검색 시 embedding-query를 사용한다.

6. Qdrant 검색
   - Qdrant Vector DB에서 cosine 유사도 기반 벡터 검색으로 세션을 찾는다.

7. 선택적 리랭킹
   - Solar Mini가 검색 상위 후보를 쿼리 관련성 순으로 재정렬한다(옵션).

핵심 전략:
LLM은 요약에 SKT A.X-K1과 Upstage Solar Pro 3를 사용하고, 경량 작업(배치 의도 분석,
탭 클러스터링, 검색 리랭킹)에는 Solar Mini를 사용한다. 임베딩은 Upstage embedding
계열(저장은 passage, 검색은 query 모델)을 사용한다. Agent 동작은 도구 호출을 강제하는
Structured Output 방식이 아니라, 프롬프트로 JSON 응답을 요청한 뒤 응답에서 JSON을
추출·검증하고 실패 시 방어적으로 fallback하는 방식으로 안정성을 확보한다.

---

## 6. MVP 구현 전략

2인 팀 기준으로 3단계 MVP를 계획한다.

### 1차 MVP

기간:
3~4주

목표:
데모에 필요한 최소 기능 구현

구현 기능:
- Chrome Extension MV3 구축
- 탭 수집
- 그룹명 자동 생성
- 세션 저장
- 기본 복원 기능

핵심:
사용자가 현재 열린 탭을 수집하고, 세션으로 저장하고, 다시 복원할 수 있어야 한다.

### 2차 MVP

기간:
추가 2~3주

목표:
서비스 핵심 UX 구현

구현 기능:
- 페이지 요약
- 세션 요약
- RAG 기반 자연어 검색
- LLM 품질 최적화

핵심:
사용자가 자연어로 과거 탐색 흐름을 검색하고 복원할 수 있어야 한다.

### 3차 MVP

기간:
추가 2주

목표:
고도화 기능 구현

구현 기능:
- AI 비교표 자동 생성
- TODO 추출
- 북마크 자동 저장
- Notion export 연동

핵심:
브라우저 세션 관리 도구를 넘어 AI Workspace로 확장한다.

---

## 7. 국내 AI 모델 선택 근거

한국어 중심 브라우저 환경에서는 자연스러운 세션 분류와 맥락 이해를 위해 한국어 특화 AI 모델이 필요하다.

후보 모델:

1. SKT A.X K1
   - 규모: 519B-A33B
   - Long Context 지원
   - Tool Calling 특화
   - Orbit 적합성 매우 높음

2. Upstage Solar Pro 3
   - 규모: 102.6B-A12B
   - 128K Long Context 지원
   - Tool Calling 지원
   - Orbit 적합성 높음

3. LG K-EXAONE
   - 규모: 236B-A23B
   - Long Context 지원
   - Tool Calling 명시는 제한적
   - Orbit 적합성 보통

4. KT Mi:dm K 2.0
   - 규모: 11.5B
   - Long Context 제한적
   - Tool Calling 업데이트 중
   - Orbit 적합성 낮음

최종 전략:
SKT A.X K1과 Upstage Solar Pro 3를 메인 LLM으로 사용하고, Solar Embedding을 임베딩 모델로 사용한다.

---

## 8. 차별화 포인트

Orbit은 기존 탭 관리 도구와 다르다.

기존 방식:
- 북마크에 직접 저장
- 탭 그룹을 수동 생성
- URL이나 제목으로만 검색
- 탭을 닫으면 맥락이 사라짐

Orbit 방식:
- 방문 이벤트를 상시 기억하고 AI가 세션으로 자동 생성·성장(Auto Session)
- 저장 판단을 하지 않고 닫아버린 탐색도 세션으로 재구성
- 자연어로 세션과 개별 방문 기록을 함께 검색(Search by Intent)
- 세션 복원으로 탐색 맥락 유지, 탐색 패턴은 Analytics로 확인

핵심 메시지:
브라우저 탭과 방문은 단순한 링크가 아니라 사용자의 사고 흐름이다. Orbit은 이 사고 흐름을 AI가 상시 기억하고 복원하는 Personal Exploration Memory다.

---

## 9. 개발 시 중요한 방향성

Orbit 개발 시 가장 중요한 것은 다음이다.

1. 먼저 Chrome Extension에서 탭 수집과 세션 저장/복원을 완성한다.
2. AI 기능은 처음부터 복잡하게 붙이지 말고, mock 또는 규칙 기반 세션 생성으로 전체 흐름을 먼저 만든다.
3. 이후 Solar Embedding, Qdrant, LLM 요약을 붙여 AI 기반 세션 분류와 검색을 구현한다.
4. Function Calling이나 Agent Action은 안정성이 중요하므로, LLM 결과를 바로 실행하지 말고 Structured Output으로 action plan을 만든 뒤 검증 후 실행한다.
5. 사용자의 브라우저 데이터를 다루므로 개인정보와 민감 정보 수집을 최소화해야 한다.

---

## 10. Orbit의 최종 목표

Orbit은 브라우저를 단순 웹 탐색 도구에서 사용자의 정보 탐색 과정과 사고 흐름을 기억하는 AI Workspace로 확장하는 것을 목표로 한다.

최종적으로 사용자는 다음과 같은 경험을 해야 한다.

“내가 전에 어떤 목적으로 어떤 자료를 보고 있었는지 AI가 기억하고, 필요한 순간 자연어로 다시 찾아서 작업을 이어갈 수 있게 해준다.”